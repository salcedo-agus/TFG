---
phase: 04-gui-fairing-controls-stand-in
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - SRC/gui/gui.py
  - SRC/gui/rocket_diagram.py
  - SRC/gui/fairing_geometry.py
autonomous: true
requirements:
  - FAIR-01
  - FAIR-03
  - FAIR-04
  - VIS-01
estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Rocket diagram renders all stage bodies with correct diameter/length (true-scale 1m=1px)"
    - "Fairing renders as ogive polygon (Constant/Tapered) or ogive+boat-tail (Hammer-Head)"
    - "Diagram updates automatically when any input changes (body mode, fairing mode, diameters, stage count)"
    - "Fairing Geometry export section shows per-stage diameter, length, volume for all modes"
    - "Diagram shows placeholder when no analysis results available"
  artifacts:
    - Enhanced RocketDiagramView.build_rocket_scene with per-stage fairing support
    - _print_results Fairing Geometry section with per-stage data
    - Input change → diagram update wiring
  key_links:
    - "gui.py _on_inputs_changed → RocketDiagramView.update_diagram()"
    - "gui.py _run → fairing_geometry.compute_fairing_geometry per stage → diagram + export"
    - "rocket_diagram.py → fairing_geometry coords for polygon rendering"
---

<objective>
Complete the diagram rendering and export for all fairing modes. Enhance RocketDiagramView to render per-stage bodies + fairing with correct geometry (ogive for Constant/Tapered, ogive+boat-tail for Hammer-Head). Wire diagram to update on any input change via _on_inputs_changed. Extend _print_results Fairing Geometry section with per-stage fairing data. Add "Run analysis to see diagram" placeholder when results stale.
</objective>

<execution_context>
@C:/Users/agusc/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/agusc/.config/opencode/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gui-fairing-controls-stand-in/04-CONTEXT.md
@.planning/phases/04-gui-fairing-controls-stand-in/04-RESEARCH.md
@SRC/gui/gui.py
@SRC/gui/rocket_diagram.py
@SRC/gui/fairing_geometry.py
@SRC/gui/constraints.py
</context>

<tasks>

<task type="auto">
  <name>Enhance RocketDiagramView for per-stage fairing rendering + placeholder</name>
  <files>
    SRC/gui/rocket_diagram.py
  </files>
  <read_first>
    SRC/gui/rocket_diagram.py (RocketDiagramView, build_rocket_scene from tracer)
    SRC/gui/fairing_geometry.py (coords format for ogive + boat-tail)
  </read_first>
  <action>
    Enhance RocketDiagramView in rocket_diagram.py:
    1. Add update_diagram(stage_data: list[dict], fairing_data: list[dict] | None, show_placeholder: bool) method. If show_placeholder: clear scene, add centered text "Run analysis to see diagram", return.
    2. Enhance build_rocket_scene (called by update_diagram when not placeholder):
       - stage_data: list of {diameter, length, ...} from Fortran results (or mock for now)
       - fairing_data: list of fairing geometry dicts (one per stage, from fairing_geometry.compute_fairing_geometry)
       - Stack stages vertically (y_offset accumulating stage lengths) in true-scale meters (1 unit = 1 meter)
       - For each stage: QGraphicsRectItem centered at x=0, width=diameter, height=length, pen=cosmetic 0.02m, brush=subtle fill
       - For fairing (typically stage 0 / top stage): use fairing_data[0]["coords"] to build QGraphicsPolygonItem (mirror left/right for closed profile). For Hammer-Head: coords include boat-tail transition to body diameter.
       - Set sceneRect to encompass full rocket + margin. Call fitInView(KeepAspectRatio).
    3. Add color constants: STAGE_BODY_COLOR, FAIRING_COLOR, FAIRING_HAMMER_COLOR for visual distinction.
    4. Ensure Y-axis: origin at fairing tip (top), stages stack downward (+Y). Flip Y in view via setTransform(QTransform.fromScale(1, -1)) so diagram renders tip-up.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'SRC'); from gui.rocket_diagram import RocketDiagramView; from PyQt6.QtWidgets import QApplication; app=QApplication([]); v=RocketDiagramView(); v.update_diagram([{'diameter':2.0, 'length':20.0}], None, True); assert v.scene().items(); print('Placeholder OK')</automated>
  </verify>
  <done>
    RocketDiagramView.update_diagram renders multi-stage bodies + fairing in true-scale; Hammer-Head shows ogive+boat-tail; placeholder text shown when no results; Y-up orientation (tip at top).
  </done>
  <reversibility>
    rating: reversible
    rationale: Rendering logic only; no changes to data flow or UI structure.
  </reversibility>
</task>

<task type="auto">
  <name>Wire diagram updates to input changes; per-stage fairing computation</name>
  <files>
    SRC/gui/gui.py
  </files>
  <read_first>
    SRC/gui/gui.py (_run: line 1034; _on_inputs_changed: line 966; _last_results storage; _last_stage_data)
    SRC/gui/fairing_geometry.py (compute_fairing_geometry interface)
    SRC/gui/rocket_diagram.py (RocketDiagramView.update_diagram)
  </read_first>
  <action>
    In gui.py, connect diagram to live updates per D-09:
    1. In _run: After run_full_pipeline returns results, extract stage_data list from results (diameter, length per stage). Compute fairing_data per stage: for each stage, call fairing_geometry.compute_fairing_geometry(body_d=stage['diameter'], fairing_mode=self.fairing_mode, fairing_diameter=self.fairing_diameter_spin.value() if visible else None). Call self.rocket_diagram.update_diagram(stage_data, fairing_data, False).
    2. In _on_inputs_changed: Add self.rocket_diagram.update_diagram([], None, True) to show placeholder immediately when any input changes (before re-run).
    3. Store fairing_mode and fairing_diameter (from spinbox if visible, else None) as MainWindow attributes for access in _run.
    4. Handle case where _last_results exists but inputs changed: _on_inputs_changed clears results and shows placeholder; _run rebuilds with new fairing params.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'SRC'); from gui.gui import MainWindow; from PyQt6.QtWidgets import QApplication; app=QApplication([]); w=MainWindow(); w.show(); w._on_inputs_changed(); assert w.rocket_diagram.scene().items(); print('Placeholder on input change OK')</automated>
  </verify>
  <done>
    Diagram updates on any input change (shows placeholder); _run computes per-stage fairing geometry and renders diagram; fairing_diameter from spinbox used for Tapered/Hammer-Head; Constant mode uses body diameter.
  </done>
  <reversibility>
    rating: reversible
    rationale: Wiring changes only; adds update calls to existing invalidation/run flow.
  </reversibility>
</task>

<task type="auto">
  <name>Extend _print_results with per-stage Fairing Geometry section</name>
  <files>
    SRC/gui/gui.py
  </files>
  <read_first>
    SRC/gui/gui.py (_print_results: lines 1016-1028 export loop; ResultCard.add_metric pattern)
    SRC/gui/fairing_geometry.py (compute_fairing_geometry return dict keys)
  </read_first>
  <action>
    Extend _print_results in gui.py per D-10 and FAIR-04:
    1. After stage data export loop (after "=======" separator), add "FAIRING GEOMETRY" section header.
    2. For each stage in _last_results (or stage_data), compute fairing geometry using same logic as diagram: fairing_geometry.compute_fairing_geometry(body_d=stage_d, fairing_mode=self.fairing_mode, fairing_diameter=self.fairing_diameter_spin.value() if self.fairing_diameter_spin.isVisible() else None).
    3. Write per-stage lines: "Stage {i}: Fairing Diameter={d:.2f} m, Length={L:.2f} m, Volume={V:.3f} m³". Use .get() with "—" default for missing keys (mirror ResultCard pattern).
    4. For Constant mode: all stages show same fairing diameter = body diameter (or max body diameter for statistical).
    5. Ensure export matches diagram data (same computation path).
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'SRC'); from gui.gui import MainWindow; from PyQt6.QtWidgets import QApplication; import tempfile, os; app=QApplication([]); w=MainWindow(); w._last_results={'stages':[{'diameter':2.0,'length':20.0},{'diameter':1.5,'length':15.0}]}; w.fairing_mode=1; f=tempfile.NamedTemporaryFile(delete=False, suffix='.txt'); f.close(); w._print_results(f.name); content=open(f.name).read(); assert 'FAIRING GEOMETRY' in content; assert 'Diameter=2.00' in content; os.unlink(f.name); print('Export OK')</automated>
  </verify>
  <done>
    .txt export contains FAIRING GEOMETRY section after stage data; per-stage fairing diameter, length, volume listed; values match diagram computation; Constant mode shows fairing = body per stage.
  </done>
  <reversibility>
    rating: reversible
    rationale: Export format extension only; adds section to existing file write.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analysis results → Diagram/Export | Fortran pipeline results drive rendering and export |
| User input → Diagram/Export | Fairing mode/diameter selections affect output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | Diagram coordinate scaling overflow | low | accept | True-scale meters; typical rockets < 100m; QGraphicsScene handles large coords |
| T-04-08 | Information Disclosure | Export file contains fairing geometry | low | accept | User-initiated export; local file; no sensitive data |
| T-04-09 | Tampering | Stale diagram shown after input change | medium | mitigate | _on_inputs_changed immediately shows placeholder; _run updates after computation |
</threat_model>

<verification>
Run GUI: change body mode → diagram shows placeholder. Run analysis → diagram renders stages + fairing (ogive for Constant/Tapered, ogive+boat-tail for Hammer-Head). Switch fairing mode → placeholder → re-run → diagram updates. Export .txt → FAIRING GEOMETRY section present with per-stage D/L/V. All three fairing modes produce correct visual and export output.
</verification>

<success_criteria>
- Diagram renders multi-stage rocket with true-scale proportions (VIS-01)
- Fairing ogive profile correct for Constant/Tapered; ogive+boat-tail for Hammer-Head
- Diagram updates on any input change (placeholder → results) (D-09)
- Export includes per-stage Fairing Geometry section (D-10, FAIR-04)
- Placeholder text shown when no results available
</success_criteria>

<output>
Create .planning/phases/04-gui-fairing-controls-stand-in/04-03-SUMMARY.md when done
</output>