---
phase: 04-gui-fairing-controls-stand-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SRC/gui/gui.py
  - SRC/gui/fairing_geometry.py
  - SRC/gui/rocket_diagram.py
autonomous: true
requirements:
  - FAIR-01
  - FAIR-03
  - FAIR-04
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "User sees Fairing Mode group in Vehicle Config tab with Constant mode radio button"
    - "Constant mode: fairing diameter equals body diameter (mock computed)"
    - "Rocket diagram renders stage bodies and fairing (constant = body) in true-scale"
    - "Fairing geometry (diameter, length, volume) appears in .txt export"
  artifacts:
    - SRC/gui/fairing_geometry.py
    - SRC/gui/rocket_diagram.py
    - Fairing mode state on MainWindow (fairing_mode, fairing_diameter)
  key_links:
    - "gui.py → fairing_geometry.compute_fairing_geometry() for mock data"
    - "gui.py → RocketDiagramView for diagram rendering"
    - "_print_results → fairing geometry dict for export"
---

<objective>
Implement the tracer slice: Constant fairing mode end-to-end. Add Fairing Mode group with Constant radio button, create Python mock fairing geometry module, add basic QGraphicsView rocket diagram showing stage bodies + fairing (same diameter as body), and include fairing geometry in .txt export. This proves the architecture: GUI → mock geometry → diagram → export.
</objective>

<execution_context>
@C:/Users/agusc/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/agusc/.config/opencode/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gui-fairing-controls-stand-in/04-CONTEXT.md
@.planning/phases/04-gui-fairing-controls-stand-in/04-RESEARCH.md
@SRC/gui/gui.py
</context>

<tasks>

<task type="tracer">
  <name>Tracer: Constant fairing mode — UI, mock geometry, diagram, export</name>
  <files>
    SRC/gui/gui.py
    SRC/gui/fairing_geometry.py
    SRC/gui/rocket_diagram.py
  </files>
  <read_first>
    SRC/gui/gui.py (lines 823-907: _build_vehicle_tab body diameter modes; line 966: _on_inputs_changed; line 1034: _run; lines 1016-1028: _print_results)
    SRC/gui/typical_data_ranges.py (pattern for auto-generated data modules)
  </read_first>
  <action>
    Implement the thinnest end-to-end path for Constant fairing mode (FAIR-01):
    1. Create SRC/gui/fairing_geometry.py with compute_fairing_geometry(body_d, fairing_mode, fairing_d=None) returning dict{diameter, length, volume, ogive_length, boat_tail_angle, coords}. For mode=1 (Constant): fairing_d = body_d, ogive_length = 3×D, volume = 0.75×π/4×D²×L, coords for ogive polygon (per D-07, RESEARCH.md Pattern 3).
    2. Create SRC/gui/rocket_diagram.py with RocketDiagramView(QGraphicsView) and RocketDiagramScene. build_rocket_scene(stage_data, fairing_data) renders stage rectangles (true-scale meters, centered at x=0) and fairing polygon from coords (per RESEARCH.md Pattern 1). Set sceneRect, fitInView, enable ScrollHandDrag for pan (disabled until Phase 5), Ctrl+wheel zoom (disabled until Phase 5).
    3. In gui.py _build_vehicle_tab: Add fairing_mode_group QGroupBox "FAIRING MODE" (hidden initially, per D-02). Add fairing_mode_const QRadioButton "Constant (same as body)" with QButtonGroup id=1. Store self.fairing_mode = 1 on MainWindow. Connect buttonToggled → _on_fairing_mode_toggled (stores mode) and _on_inputs_changed (invalidates results). Add header label "Fairing" above group (per D-03).
    4. In gui.py _run: After run_full_pipeline returns, call fairing_geometry.compute_fairing_geometry with body_diameter (from results or user input), fairing_mode=1. Pass fairing_data to RocketDiagramView.build_rocket_scene. Add RocketDiagramView to vehicle tab layout below mode groups.
    5. In gui.py _print_results: After stage data export, append "Fairing Geometry" section with per-stage fairing diameter, length, volume (mock: same for all stages for now). Use ResultCard.add_metric pattern with .get() defaults.
    6. Ensure _on_inputs_changed connects fairing_mode_group.buttonToggled for invalidation.
  </action>
  <verify>
    <automated>python -c "from SRC.gui.fairing_geometry import compute_fairing_geometry; r=compute_fairing_geometry(2.0, 1); assert r['diameter']==2.0 and abs(r['ogive_length']-6.0)<0.01 and r['volume']>0; print('OK:', r)"</automated>
  </verify>
  <done>
    Constant fairing mode radio appears in Vehicle Config tab; selecting it shows fairing diameter = body diameter in diagram; .txt export includes Fairing Geometry section with diameter/length/volume; no Fortran changes.
  </done>
  <reversibility>
    rating: reversible
    rationale: Adds new UI group, new modules, and export section — all removable without affecting existing body diameter modes or Fortran bridge.
  </reversibility>
</task>

<task type="auto">
  <name>Fairing group layout: 60/40 split, visibility contract, header labels</name>
  <files>
    SRC/gui/gui.py
  </files>
  <read_first>
    SRC/gui/gui.py (lines 823-907: _build_vehicle_tab layout structure, body mode group, diameter_spin visibility pattern)
  </read_first>
  <action>
    Complete the Fairing Mode group layout per D-01, D-02, D-03:
    1. Wrap body mode group and fairing mode group in a QHBoxLayout with stretch factors 60/40 (body/fairing) — per D-01.
    2. Fairing group initially hidden (self.fairing_mode_group.setVisible(False)). Connect body mode group buttonToggled to a slot that shows fairing group when any body mode is selected — per D-02.
    3. Add QLabel "Body" above body mode group and QLabel "Fairing" above fairing mode group — per D-03.
    4. Mirror existing diameter_spin pattern: fairing_diameter_spin QDoubleSpinBox with suffix " m", decimals=2, singleStep=0.1, initially hidden. For Constant mode, spinbox stays hidden (no user input needed). Connect valueChanged → _on_inputs_changed.
    5. Set fairing_mode_const.setChecked(True) as default.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'SRC'); from gui.gui import MainWindow; from PyQt6.QtWidgets import QApplication; app=QApplication([]); w=MainWindow(); w.show(); assert w.fairing_mode_group.isVisible()==False; w.body_mode_stat.setChecked(True); app.processEvents(); assert w.fairing_mode_group.isVisible()==True; print('Layout OK')</automated>
  </verify>
  <done>
    Body/Fairing groups side-by-side 60/40; Fairing group hidden until body mode selected; "Body"/"Fairing" headers present; Constant mode radio checked by default; fairing spinbox hidden for Constant mode.
  </done>
  <reversibility>
    rating: reversible
    rationale: Layout and visibility changes only — no logic alterations to existing body diameter modes.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Fairing geometry | User selects fairing mode and diameter; mock computation runs in-process |
| GUI → Export file | Application writes .txt results to user-selected path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | fairing_geometry.compute_fairing_geometry | low | mitigate | Input validation in compute_fairing_geometry (positive diameters, valid mode enum); spinbox enforces min/max |
| T-04-02 | Tampering | RocketDiagramView scene coordinates | low | accept | True-scale rendering uses internal math; no external input to scene construction |
| T-04-03 | Tampering | _print_results file write | low | mitigate | User controls save path via QFileDialog; export is write-only, no parsing |
| T-04-SC | Tampering | pip install qt-material | high | mitigate | Package legitimacy audit in RESEARCH.md (qt-material: OK); install only from PyPI |
</threat_model>

<verification>
Run GUI, select body mode → fairing group appears. Select Constant fairing mode → run analysis → diagram shows fairing same diameter as body stages. Export results → .txt contains Fairing Geometry section with D/L/V per stage.
</verification>

<success_criteria>
- Fairing Mode group renders correctly in Vehicle Config tab (D-01, D-02, D-03)
- Constant mode computes fairing geometry matching body diameter (FAIR-01)
- RocketDiagramView renders stage bodies + fairing ogive in true-scale (D-08)
- .txt export includes Fairing Geometry section (D-10, FAIR-04)
- All new modules import without error
</success_criteria>

<output>
Create .planning/phases/04-gui-fairing-controls-stand-in/04-01-SUMMARY.md when done
</output>