---
phase: 04-gui-fairing-controls-stand-in
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - SRC/gui/gui.py
  - SRC/gui/constraints.py
  - SRC/gui/fairing_geometry.py
autonomous: true
requirements:
  - FAIR-01
  - FAIR-03
  - FAIR-04
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Tapered mode: fairing diameter ≤ last stage body diameter (user-specified via spinbox)"
    - "Hammer-Head mode: fairing diameter ≥ body diameter (user-specified via spinbox)"
    - "Constraint matrix enforced: body mode determines available fairing modes (D-04)"
    - "Fairing spinbox range updates dynamically when body/fairing mode changes (D-05)"
    - "FAIR-02 (User-specified fairing with body adjustment) is NOT implemented (deferred per D-06)"
  artifacts:
    - SRC/gui/constraints.py (FairingConstraintValidator class)
    - Tapered and Hammer-Head radio buttons in gui.py
    - Dynamic spinbox range logic
  key_links:
    - "constraints.py → FairingConstraintValidator.CONSTRAINTS matrix (D-04)"
    - "gui.py body_mode_group.buttonToggled → validator._on_body_mode_changed"
    - "gui.py fairing_mode_group.buttonToggled → validator._on_fairing_mode_changed"
    - "fairing_geometry.py → compute_fairing_geometry handles modes 2, 3"
---

<objective>
Implement the full fairing constraint matrix (D-04) and Tapered/Hammer-Head modes (FAIR-03). Create FairingConstraintValidator to dynamically manage allowed fairing modes per body mode and update spinbox min/max. Extend fairing_geometry.py for modes 2 and 3. Wire validator to both mode groups.
</objective>

<execution_context>
@C:/Users/agusc/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/agusc/.config/opencode/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gui-fairing-controls-stand-in/04-CONTEXT.md
@.planning/phases/04-gui-fairing-controls-stand-in/04-RESEARCH.md
@SRC/gui/gui.py
@SRC/gui/fairing_geometry.py
</context>

<tasks>

<task type="auto">
  <name>Create FairingConstraintValidator with D-04 constraint matrix</name>
  <files>
    SRC/gui/constraints.py
  </files>
  <read_first>
    SRC/gui/fairing_geometry.py (mode constants: 1=Constant, 2=Tapered, 3=Hammer-Head)
    SRC/gui/gui.py (body_mode_group ids: 1=Statistical, 2=Constant, 3=User-specified; fairing_mode_group ids)
  </read_first>
  <action>
    Create SRC/gui/constraints.py with FairingConstraintValidator class per RESEARCH.md Pattern 2:
    1. CONSTRAINTS matrix mapping body_mode_id → {fairing_mode_id: (label, min_expr, max_expr)}:
       - Body 1 (Statistical): Fairing 1 (Constant, no spinbox), Fairing 2 (Tapered, max=last_body_d)
       - Body 2 (Constant): Fairing 1 (Constant, no spinbox), Fairing 3 (Hammer-Head, min=body_d)
       - Body 3 (User-specified): Fairing 1 (Constant, no spinbox) only — FAIR-02 deferred per D-06
    2. __init__(body_mode_group, fairing_mode_group, fairing_spin, get_body_diameters_fn, get_last_body_d_fn, get_body_d_fn)
    3. _on_body_mode_changed(btn, checked): if checked, rebuild fairing mode radios for allowed modes, select first, call _update_fairing_spinbox_range()
    4. _on_fairing_mode_changed(btn, checked): if checked, call _update_fairing_spinbox_range()
    5. _rebuild_fairing_modes(body_mode): clear fairing_mode_group buttons, create radios for allowed modes from CONSTRAINTS, add to button group with same ids (1,2,3), set first checked
    6. _update_fairing_spinbox_range(): resolve min/max expressions ("body_d", "last_body_d", None) using getter functions; if both None → hide spinbox; else setRange(min, max), show spinbox, setSuffix(" m")
    7. Tooltip on spinbox when at boundary: "Fairing diameter must be ≤ last stage body diameter (Tapered)" or "≥ body diameter (Hammer-Head)"
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'SRC'); from gui.constraints import FairingConstraintValidator; from PyQt6.QtWidgets import QButtonGroup, QDoubleSpinBox; print('Constraints module loads OK')</automated>
  </verify>
  <done>
    constraints.py exists with FairingConstraintValidator class; CONSTRAINTS matrix matches D-04 exactly; _rebuild_fairing_modes repopulates fairing radios correctly per body mode; spinbox range updates dynamically.
  </done>
  <reversibility>
    rating: costly
    rationale: Validator centralizes constraint logic spanning multiple widgets; changes here affect all fairing mode interactions. Reversible but requires careful unwiring.
  </reversibility>
</task>

<task type="auto">
  <name>Add Tapered and Hammer-Head radio buttons; wire validator in gui.py</name>
  <files>
    SRC/gui/gui.py
  </files>
  <read_first>
    SRC/gui/gui.py (lines 823-907: _build_vehicle_tab fairing group area; body_mode_group setup; _on_inputs_changed)
    SRC/gui/constraints.py (FairingConstraintValidator interface)
  </read_first>
  <action>
    In gui.py _build_vehicle_tab, extend fairing mode group:
    1. Add fairing_mode_tapered QRadioButton "Tapered (smaller than body)" id=2, fairing_mode_hammer QRadioButton "Hammer-Head (larger than body)" id=3 to fairing_mode_buttons QButtonGroup.
    2. Add helper labels under each radio (per D-03 style): "Tapered: fairing diameter ≤ last stage body diameter (user-specified)", "Hammer-Head: fairing diameter ≥ body diameter (user-specified)".
    3. Create FairingConstraintValidator instance in MainWindow.__init__ or _build_vehicle_tab, passing:
       - body_mode_group, fairing_mode_buttons
       - fairing_diameter_spin
       - get_body_diameters_fn: lambda: [s.get('diameter', 2.0) for s in self._last_stage_data] (or from results)
       - get_last_body_d_fn: lambda: self._last_stage_data[-1].get('diameter', 2.0) if self._last_stage_data else 2.0
       - get_body_d_fn: lambda: max(s.get('diameter', 2.0) for s in self._last_stage_data) if self._last_stage_data else 2.0
    4. Connect body_mode_group.buttonToggled → validator._on_body_mode_changed
    5. Connect fairing_mode_buttons.buttonToggled → validator._on_fairing_mode_changed
    6. Connect fairing_diameter_spin.valueChanged → _on_inputs_changed (already in tracer)
    7. Ensure fairing_mode state (self.fairing_mode) updates in _on_fairing_mode_toggled (read from fairing_mode_buttons.checkedId())
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'SRC'); from gui.gui import MainWindow; from PyQt6.QtWidgets import QApplication; app=QApplication([]); w=MainWindow(); w.show(); w.body_mode_stat.setChecked(True); app.processEvents(); assert w.fairing_mode_buttons.checkedId()==1; w.body_mode_const.setChecked(True); app.processEvents(); modes=[w.fairing_mode_buttons.button(i).text() for i in [1,3] if w.fairing_mode_buttons.button(i)]; assert 'Hammer-Head' in ' '.join(modes); print('Constraint wiring OK')</automated>
  </verify>
  <done>
    Tapered and Hammer-Head radios appear in fairing group; body Statistical → shows Constant + Tapered; body Constant → shows Constant + Hammer-Head; body User-specified → shows Constant only; spinbox appears with correct min/max for Tapered/Hammer-Head; hidden for Constant.
  </done>
  <reversibility>
    rating: costly
    rationale: Adds validation logic wired across body/fairing mode groups and spinbox; modifies _build_vehicle_tab significantly.
  </reversibility>
</task>

<task type="auto">
  <name>Extend fairing_geometry.py for Tapered and Hammer-Head modes</name>
  <files>
    SRC/gui/fairing_geometry.py
  </files>
  <read_first>
    SRC/gui/fairing_geometry.py (compute_fairing_geometry from tracer; OGIVE_CALIBER=3.0, VOLUME_FACTOR=0.75, BOAT_TAIL_DEG=10.0)
  </read_first>
  <action>
    Extend compute_fairing_geometry in fairing_geometry.py for modes 2 and 3 per D-07 and RESEARCH.md Pattern 3:
    1. Mode 2 (Tapered): fairing_d = fairing_diameter (user input, validated ≤ last_body_d). ogive_length = 3×fairing_d. volume = 0.75×π/4×fairing_d²×ogive_length. coords for ogive polygon (same as Constant but with fairing_d).
    2. Mode 3 (Hammer-Head): fairing_d = fairing_diameter (user input, validated ≥ body_d). ogive_length = 3×fairing_d. Add boat-tail transition: bt_angle = 10°, bt_length = (fairing_d - body_d) / 2 / tan(bt_angle). coords: ogive profile + boat-tail segment to body_d/2. volume includes ogive + cylindrical approx.
    3. Return dict with boat_tail_angle=10.0 for mode 3, 0.0 otherwise.
    4. Add unit-testable helper functions: _ogive_coords(diameter, length), _boat_tail_coords(fairing_d, body_d, angle_deg).
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'SRC'); from gui.fairing_geometry import compute_fairing_geometry; r=compute_fairing_geometry(2.0, 2, 1.5); assert r['diameter']==1.5 and r['ogive_length']==4.5; r=compute_fairing_geometry(2.0, 3, 3.0); assert r['diameter']==3.0 and r['boat_tail_angle']==10.0 and len(r['coords'])>20; print('Modes 2,3 OK')</automated>
  </verify>
  <done>
    compute_fairing_geometry handles mode 2 (Tapered) with user fairing_d ≤ body_d; mode 3 (Hammer-Head) with user fairing_d ≥ body_d + boat-tail coords; returns correct diameter, length, volume, coords for all three modes.
  </done>
  <reversibility>
    rating: reversible
    rationale: Pure math module; new modes added as branches; no side effects.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Constraint validator | Body/fairing mode selections drive spinbox ranges |
| User input → Fairing geometry | Spinbox value feeds mock computation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | FairingConstraintValidator constraint bypass | medium | mitigate | Validator is single source of truth; spinbox.setRange() enforces bounds at widget level; no API bypass |
| T-04-05 | Tampering | Invalid fairing/body combo in export | low | mitigate | Validator prevents invalid combos; export reads validated state |
| T-04-06 | Denial of Service | Spinbox range oscillation on mode change | low | accept | Single validator instance; signals connected once; no feedback loops |
</threat_model>

<verification>
Run GUI: select Statistical body → fairing shows Constant + Tapered; spinbox for Tapered max = last stage body D. Select Constant body → fairing shows Constant + Hammer-Head; spinbox min = body D. Select User-specified body → fairing shows Constant only. Enter valid/invalid values in spinbox → range enforced. Export shows correct fairing geometry per mode.
</verification>

<success_criteria>
- Constraint matrix (D-04) fully enforced in UI
- Tapered mode: fairing D ≤ last stage body D (FAIR-03 partial)
- Hammer-Head mode: fairing D ≥ body D (FAIR-03)
- FAIR-02 not implemented (D-06 honored)
- Spinbox tooltip shows constraint message at boundaries (D-05)
- fairing_geometry.py computes correct geometry for all 3 modes
</success_criteria>

<output>
Create .planning/phases/04-gui-fairing-controls-stand-in/04-02-SUMMARY.md when done
</output>