---
phase: 03-bridge-correctness-fixes
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - SRC/test/test_rocket_lib.py
autonomous: true
requirements: [FIX-01]

estimate:
  tokens: 6000
  raw_tokens: 5000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Stage-1 m0 is finite and strictly greater than payload_mass + PAF (rm_L formula: 0.0755*payload + 50)"
    - "Every stage k_L falls strictly between 0 and 1"
    - "The full 23-test suite passes green against the rebuilt DLL (baseline before dedup)"
  artifacts:
    - path: "SRC/test/test_rocket_lib.py"
      provides: "D-02 conservative-bounds regression test for rm_L initialization"
      contains: "test_conservative_bounds"
  key_links:
    - from: "SRC/test/test_rocket_lib.py::test_conservative_bounds"
      to: "SRC/interface/rocket_lib.py::run_full_pipeline"
      via: "_baseline() helper calls run_full_pipeline which calls Payload_Mass_calculator (the sole rm_L authority per D-01)"
      pattern: "run_full_pipeline.*N3_CONFIG"
---

<objective>
Add a D-02 conservative-bounds regression test proving `Rocket%rm_L` is correctly
initialized on the ctypes/GUI path (FIX-01), then verify the full suite stays green
against a freshly rebuilt DLL. This establishes the pre-dedup baseline: all 23+ tests
pass, and the new bounds guard catches any future rm_L regressions.

Purpose: FIX-01 verification (per D-03, rm_L consolidation is folded into the dedup
change in 03-02; this plan proves the single formula source works before we remove the
duplicate path). The bounds test becomes a permanent regression sentinel.

Output: Updated `test_rocket_lib.py` with the `test_conservative_bounds` method;
green baseline on the full suite (23 existing + 1 new = 24).
</objective>

## Flagged Assumptions (spec-less probe fallback — EDGE_ABSENT, never silently dropped)

> No SPEC file exists for this phase. The edge probe surfaced the following UNRESOLVED
> items (status stays `unresolved` per §A; surfaced here as the execution assumptions):

| Req | Probe category | Flagged assumption (this phase's stance) |
|-----|----------------|------------------------------------------|
| FIX-01 | unclassified (manual review) | Resolved-by-design assumption: `run_full_pipeline` calls `Payload_Mass_calculator` (C_Interface.f90:169) which writes `Rocket%rm_L` — the single formula source. The D-02 bounds test (Task 1) is the permanent behavioral guard. If the bounds test fails, rm_L initialization is broken — treat as RED, never suppress. |
| FIX-01 | empty / null input | Scope-limited assumption: n_stages is GUI-enforced to 1-3; the bounds test runs on the N3_CONFIG fixture. No empty-input path exists on the surviving bridge entry. |

**Prohibition recall (per §B, run in-prompt):** No values/safety/ethics prohibitions kept.
The protestable surfaces are all engineering hygiene (must-not-retain-inline-formula,
must-not-add-a-second-entry, must-not-reintroduce-hardcoded-path) — the precision stage
drops routine-engineering items per the protocol. `must_haves.prohibitions` is therefore
empty, which is the correct precision outcome for a pure engineering-consolidation phase.

<execution_context>
@C:/Users/agusc/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/agusc/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bridge-correctness-fixes/03-CONTEXT.md
@.planning/phases/03-bridge-correctness-fixes/03-RESEARCH.md
@.planning/phases/03-bridge-correctness-fixes/03-PATTERNS.md
@.planning/phases/02-full-pipeline-exposure/02-02-SUMMARY.md
@SRC/test/test_rocket_lib.py
@SRC/test_call.py
@SRC/interface/C_Interface.f90
</context>

<tasks>

<task type="tracer">
  <name>D-02 conservative-bounds regression test for rm_L (FIX-01 sentinel)</name>
  <files>SRC/test/test_rocket_lib.py</files>
  <read_first>SRC/test/test_rocket_lib.py</read_first>
  <action>
Add a new test class `ConservativeBoundsGuard` to `test_rocket_lib.py` (per D-02,
D-03). This class contains one test method `test_conservative_bounds_n3` that:

1. Calls `self._baseline()` using the `N3_CONFIG` dict (3-stage, LH2/LOX, orbit 500 km,
   payload 5000 kg) via the `_baseline(**overrides)` helper inherited pattern — define
   the helper if not inherited, or call `run_full_pipeline(**N3_CONFIG)` directly.

2. Computes PAF from the Payload_Mass_calc.f90 eq. 11 formula:
   `paf = 0.0755 * N3_CONFIG["payload_mass"] + 50.0`

3. Asserts stage-1 `m0` is finite (`math.isfinite`) AND strictly greater than
   `N3_CONFIG["payload_mass"] + paf` — catches uninitialized/garbage rm_L.

4. Asserts every stage `k_L > 0.0` AND `k_L < 1.0` — conservative physical bounds
   that cannot false-fail on valid inputs (probe data from RESEARCH.md:339 confirms).

The test class uses `unittest.TestCase` and imports `math` (already imported). It
references `N3_CONFIG` (module-level dict at line 30) and `run_full_pipeline` (already
imported at line 23). Do NOT touch the existing test classes (`RunFullPipelineContract`,
 `RunFullPipelineVariantDomains`, `RunStagingWrapperContract`) — they stay untouched
until 03-02 removes the last one.

Guard against `self._baseline` not existing: if the test class is standalone (not
inheriting from `RunFullPipelineVariantDomains`), call `run_full_pipeline(**N3_CONFIG)`
directly rather than relying on the helper.
  </action>
  <verify>
    <automated>python -m unittest discover -s SRC/test -p "test_rocket_lib.py" -v 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
- `test_rocket_lib.py` contains class `ConservativeBoundsGuard` with method `test_conservative_bounds_n3`
- The method calls `run_full_pipeline(**N3_CONFIG)` and asserts m0, paf bounds, and k_L ∈ (0,1)
- All 24 tests (10 existing + 10 RunFullPipelineContract+Variants + 1 RunStagingWrapperContract + 1 new + 2 more existing) pass green
- The `from rocket_lib import` line is NOT modified (still imports both `run_full_pipeline` and `run_staging` — dedup is 03-02)
  </acceptance_criteria>
  <done>test_conservative_bounds_n3 exists and passes; full suite green at 24 tests (or current-count + 1)</done>
  <reversibility rating="reversible">Adding a test class is additive; no existing code modified.</reversibility>
</task>

<task type="auto">
  <name>Clean DLL rebuild + full suite baseline</name>
  <files>SRC/test/test_rocket_lib.py</files>
  <read_first>SRC/Makefile</read_first>
  <precondition>gfortran available on PATH at C:/TDM-GCC-64/bin; build/librocket.dll present from Phase 2</precondition>
  <action>
Perform a clean rebuild of the DLL and run the full test suite to establish the
pre-dedup baseline:

1. Run `make clean` then `make all` in `SRC/` to produce a fresh `build/librocket.dll`.
   This is a verification-only step: no source changes (the only file touched this plan
   is `test_rocket_lib.py`, already updated in Task 1).

2. Run the full test suite: `python -m unittest discover -s SRC/test -p "test_*.py"` —
   both `test_rocket_lib.py` and `test_gui_full_pipeline.py` must be green.

3. If `test_gui_full_pipeline.py` fails (it should not — it uses `run_full_pipeline`
   and touches no `run_staging` code), investigate and report but do NOT fix out of scope.

4. Record the test count and pass/fail status in the commit message or output for
   traceability.
  </action>
  <verify>
    <automated>make all -C SRC 2>&amp;1 &amp;&amp; python -m unittest discover -s SRC/test -p "test_*.py" 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
- `make all` compiles C_Interface.f90 and links librocket.dll with zero new warnings
- Full suite (`test_*.py`) passes with all tests green
- Test count is 24 (23 baseline + 1 new bounds test) or current-count + 1
- No source files modified beyond `test_rocket_lib.py` (Task 1)
  </acceptance_criteria>
  <done>Fresh DLL built; full suite green; baseline recorded</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DLL load (ctypes.CDLL) | Python loads build/librocket.dll — the trust boundary for binary provenance |
| Build directory | build/ is user-writable; DLLs loaded via absolute path, not cwd or PATH |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01-01 | Tampering | build/librocket.dll | low | accept | Local dev toolchain; DLL loaded via absolute path from the repo build/ dir. No external input to the DLL beyond typed scalars/arrays. |
| T-03-01-02 | Information Disclosure | test_rocket_lib.py | low | accept | Test file contains no secrets; N3_CONFIG is a hardcoded test fixture with no sensitive data. |
</threat_model>

<verification>
- Plan 03-01 overall: full suite green with 24 tests; fresh DLL compiled from current Fortran source
- The bounds test specifically guards FIX-01: if rm_L becomes uninitialized/garbage, m0 < payload+PAF or k_L out of (0,1) will fail
</verification>

<success_criteria>
1. `test_conservative_bounds_n3` method exists and passes
2. Full test suite (both files) green against freshly rebuilt DLL
3. No source files modified except `test_rocket_lib.py`
4. Pre-dedup baseline established for 03-02 comparison
</success_criteria>

<output>
Create `.planning/phases/03-bridge-correctness-fixes/03-01-SUMMARY.md` when done
</output>
