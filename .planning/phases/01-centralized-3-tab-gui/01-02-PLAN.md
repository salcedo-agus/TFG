---
phase: 01-centralized-3-tab-gui
plan: 2
slug: centralized-3-tab-gui
summary: Rework mission inputs in the Setup tab (remove dv_spin per D-10, add orbit-height spinbox + read-only auto-ΔV label + interim V_circ helper) and build the Vehicle Configuration tab (diameter modes 1/2/3 with user-specified input box, state stored for Phase 2).
type: execute
wave: 2
depends_on:
  - 01-01
files_created: []
files_modified:
  - SRC/gui/gui.py
autonomous: true
requirements:
  - GUI-05
  - GUI-06
  - GUI-07
phase_req_ids:
  - GUI-05
  - GUI-06
  - GUI-07
user_setup: []
estimate:
  tokens: 35000
  raw_tokens: 21000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Setup tab Mission Parameters grid: row 0 'Orbit Height (km)' QDoubleSpinBox 100.0-2000.0, decimals 1, single step 10.0, default 500.0; row 1 pl_spin verbatim (1.0-1,000,000, default 5000.0); row 2 n_stages_spin verbatim (1-3, default 3); row 3 read-only 'ΔV (auto)' label {:.2f} km/s in TEXT_SEC 12px (UI-SPEC:166-169)."
    - "dv_spin fully removed: `rg dv_spin SRC/gui/gui.py` matches nothing — no ΔV input exists anywhere in the GUI (D-10, GUI-05)."
    - "The ΔV (auto) label updates whenever orbit height changes and the same value feeds run_staging(delta_v=...) (gui.py:936) and the export header/filename (_print_results gui.py:869, 872, 885) — ΔV is never hand-entered."
    - "_auto_delta_v() mirrors Orbit_calc.f90:9-10 with g_0=9.80665, Radius=6378.0 (Typical_Data.f90:3-5), documented as interim pending Phase 2 pipeline ΔV (PIPE-01); no other physics duplication."
    - "Vehicle Configuration tab: header + subtitle + divider; DIAMETER MODE group with exactly 3 mutually-exclusive QRadioButtons — 'Statistically determined' (stored int 1, default checked, mirrors Diameter_setup=1), 'Constant' (2), 'User-specified' (3) — each with its 11px TEXT_SEC helper line (UI-SPEC:176-186)."
    - "State-toggle: diameter input box (QDoubleSpinBox 0.5-20.0, decimals 2, step 0.1, default 2.00, label 'User-Specified Diameter (m)') is visible only when mode 3 is checked, hidden for modes 1/2; the value persists across mode toggles and tab switches (widgets never rebuilt) (D-12, GUI-07)."
    - "Diameter mode int 1/2/3 + spinbox value stored as MainWindow state for Phase 2 handoff; NOT wired into run_staging this phase (Pitfall 8)."
  artifacts:
    - "SRC/gui/gui.py — reworked mission grid (orbit_height, ΔV auto label, no dv_spin)"
    - "SRC/gui/gui.py — _auto_delta_v() helper + _update_auto_dv_label()"
    - "SRC/gui/gui.py — _build_vehicle_tab page (radios + helpers + diameter_spin)"
  key_links:
    - "orbit_height.valueChanged -> _on_inputs_changed + _update_auto_dv_label (fan-out pattern gui.py:817-819)"
    - "_auto_delta_v() -> run_staging delta_v arg and _print_results header/filename"
    - "mode radio toggled -> diameter_spin.setVisible; self.diameter_mode int kept for Phase 2"
  prohibitions:
    - "No ΔV input of any kind may exist after this plan — the removed dv_spin must not be re-added and no editable ΔV widget may be introduced (D-10)."
    - "The GUI must not read config.txt (D-14) and must not reimplement staging/geometry physics in Python beyond the marked-interim _auto_delta_v one-liner (PIPE-02)."
    - "Orbit height and diameter inputs stay widget-bounded (100-2000 km; 0.5-20 m) — no free-text entry, no eval/exec of user input (ASVS V5)."
    - "Diameter mode semantics must map exactly to Fortran 1/2/3 (Geometry_calc.f90:92-105); mode 1 must be the default checked state (mirrors config.txt Diameter_setup = 1)."
    - "Diameter box visibility contract must hold (visible only in mode 3) and the value must persist across toggles — the widgets are never rebuilt."
---

<objective>
Add the mission inputs (orbit height, internal ΔV) and the Vehicle Configuration tab (diameter modes + input box).

Purpose: Fulfill GUI-05 (mission inputs in the GUI, ΔV computed internally — never hand-entered) and GUI-06/GUI-07 (three diameter modes mirroring Fortran `diameter_setup` 1/2/3, with the user-specified box). The D-10 trap is handled by the interim `_auto_delta_v()` helper so `run_staging` and `_print_results` keep working after `dv_spin` disappears.
Output: A Setup tab with orbit height + read-only auto-ΔV (no ΔV spinbox anywhere), and a populated Vehicle Configuration tab storing mode 1/2/3 + diameter for Phase 2 handoff (not wired into `run_staging` — Pitfall 8).
</objective>

<execution_context>
@C:/Users/agusc/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/agusc/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-centralized-3-tab-gui/01-CONTEXT.md
@.planning/phases/01-centralized-3-tab-gui/01-RESEARCH.md
@.planning/phases/01-centralized-3-tab-gui/01-UI-SPEC.md
@.planning/phases/01-centralized-3-tab-gui/01-PATTERNS.md
@SRC/gui/gui.py

# Plan 2 of 3 — mission inputs + Vehicle Configuration (GUI-05, GUI-06, GUI-07)
Depends on 01-01 (shell + relocated Setup tab, incl. dv_spin at its new location). The Fortran semantics mirrored here: `diameter_setup` 1/2/3 + `user_defined_diameter` (Typical_Data.f90:49-50, 803-813; Geometry_calc.f90:92-105); V_circ formula (Orbit_calc.f90:9-10).
</context>

<tasks>

<task type="auto">
  <name>01-02-T1: Mission inputs rework — orbit height + auto ΔV (D-09, D-10, GUI-05)</name>
  <commit>feat(gui): mission inputs — orbit height spinbox and internal ΔV (D-10)</commit>
  <files>SRC/gui/gui.py</files>
  <action>
    In the relocated mission group (Setup tab, ex-gui.py:742-768): REMOVE the `dv_spin` construction (ex-gui.py:746-752) and its grid row, and remove its valueChanged connection (ex-gui.py:817). Add at grid row 0 a QDoubleSpinBox named `self.orbit_height` — range 100.0-2000.0, decimals 1, single step 10.0, default 500.0, label "Orbit Height (km)" (UI-SPEC:166; bounds per ASVS V5). Keep `pl_spin` row 1 and `n_stages_spin` row 2 verbatim.
    Add at grid row 3 a read-only QLabel named `self.auto_dv_label` — "ΔV (auto)" + value text f"{self._auto_delta_v():.2f} km/s", styled TEXT_SEC 12px; NOT editable, no spinbox (UI-SPEC:169).
    Add the helper `_auto_delta_v(self)` — interim V_circ mirror of Orbit_calc.f90:9-10 with constants g_0 = 9.80665, Radius = 6378.0 (Typical_Data.f90:3-5): r = Radius + orbit_height; return (g_0 * Radius**2 / (r * 1000.0)) ** 0.5. Docstring marks it interim, replaced by the pipeline ΔV in Phase 2 (PIPE-01) — no other physics duplication (PIPE-02).
    Add `_update_auto_dv_label(self)` setting the label text from `_auto_delta_v()`.
    Wire (fan-out pattern ex-gui.py:817-819): `self.orbit_height.valueChanged` -> `self._on_inputs_changed` AND `self._update_auto_dv_label` (UI-SPEC:174-175).
    Swap the three `dv_spin` value reads (D-10 trap): `_run` delta_v argument (ex-gui.py:936) -> `self._auto_delta_v()`; `_print_results` local dv (ex-gui.py:869) -> `self._auto_delta_v()`; Delta-V header line (ex-gui.py:885) -> same auto value. Keep the filename pattern `staging_{n}stage_dv{dv:.1f}_pl{pl}kg.txt` (ex-gui.py:872) — dv is now the computed value (UI-SPEC:118). Never hand-entered anywhere.
  </action>
  <verify>
    <automated>python -m py_compile SRC/gui/gui.py</automated>
    <human-check>`make gui` in SRC/: Setup shows Orbit Height (default 500.0), Payload, Stages, and a read-only "ΔV (auto)" label; changing orbit height updates the label, clears Results, and disables Save; Run works (delta_v from the auto value); Save exports a .txt whose Delta-V line equals the label value and whose filename follows staging_{n}stage_dv{dv:.1f}_pl{pl}kg.txt.</human-check>
    <automated>`rg "dv_spin" SRC/gui/gui.py` must match nothing (exit 1, empty output).</automated>
  </verify>
  <done>No ΔV input exists anywhere in the GUI; orbit height bounded 100-2000 km drives the read-only auto-ΔV label, the solver call, and the export header/filename; py_compile passes.</done>
  <reversibility rating="costly">dv_spin removal permanently changes the UI contract and export content (Delta-V line now auto-computed); revert restores the old behavior — gate: the export file must stay identical to the pre-rework format except that the Delta-V line reads the computed value (never a hand entry), and the app must remain fully runnable after this commit (all three dv_spin reads swapped in the same commit).</reversibility>
  <dependencies>01-01-T2 and 01-01-T3 (relocated mission group + relocated _print_results/_run).</dependencies>
</task>

<task type="auto">
  <name>01-02-T2: Build Vehicle Configuration tab (D-11, D-12, GUI-06, GUI-07)</name>
  <commit>feat(gui): add Vehicle Configuration tab with diameter modes</commit>
  <files>SRC/gui/gui.py</files>
  <action>
    Fill `_build_vehicle_tab` (page container exists from 01-01-T1): header mirroring the Setup header pattern (ex-gui.py:732-739) — "VEHICLE CONFIGURATION" 20px/800 + subtitle "Diameter sizing mode" 12px TEXT_SEC + divider QFrame.
    Build a QGroupBox "DIAMETER MODE" containing 3 mutually-exclusive QRadioButtons (QButtonGroup for exclusivity), in order (UI-SPEC:179-186): "Statistically determined" (stored int 1, setChecked(True) by default — mirrors config.txt Diameter_setup = 1), "Constant" (2), "User-specified" (3 — CONTEXT D-11 label; Fortran calls it "Fairing requirement", semantics identical per A4). Each radio followed by its 11px TEXT_SEC helper line (UI-SPEC:121 copy: "Statistically determined: each stage diameter from propellant regression curves on stage mass" / "Constant: all stages take the widest statistical diameter" / "User-specified: all stages take the diameter entered below"). Construction style modeled on the mission-group idiom (ex-gui.py:742-768) and StageInputWidget._build (gui.py:331-347).
    Add `self.diameter_spin` — QDoubleSpinBox "User-Specified Diameter (m)" — range 0.5-20.0, decimals 2, single step 0.1, default 2.00 (mirrors config.txt User_defined_diameter = 2.0), placed under the mode group, `setVisible(False)` initially.
    Wire the visibility contract: `self.mode_user.toggled.connect(self.diameter_spin.setVisible)` — mode 3 checked -> box visible; modes 1/2 -> hidden (UI-SPEC:188).
    Store state for Phase 2: keep `self.diameter_mode` (int 1/2/3, updated on radio toggled) and read the spinbox value on demand; do NOT wire into `run_staging` (Pitfall 8, UI-SPEC:187). Radio QSS already present from 01-01-T1 STYLE block.
  </action>
  <verify>
    <automated>python -m py_compile SRC/gui/gui.py</automated>
    <human-check>`make gui` in SRC/: tab 2 shows header + 3 radios + helper lines; "Statistically determined" is checked by default; selecting "User-specified" reveals the diameter box; selecting "Constant"/"Statistically determined" hides it; toggling back to mode 3 restores the previous diameter value; switching tabs does not reset any vehicle-config state; Run from Setup is unaffected by this tab.</human-check>
  </verify>
  <done>Vehicle Configuration tab offers exactly the 3 diameter modes mapped to ints 1/2/3 with the input box visibility contract and value persistence; state stored on MainWindow; run_staging untouched by this tab.</done>
  <reversibility rating="reversible">Additive new tab — no existing behavior touched; git revert removes it cleanly.</reversibility>
  <dependencies>01-01-T1 (page container) and 01-02-T1 (for a consistent Setup tab before manual verification).</dependencies>
</task>

</tasks>

## Verification (per RESEARCH "Validation Architecture")

- Per-task commit gate: `python -m py_compile SRC/gui/gui.py`.
- GUI smoke via `make gui` in `SRC/` (builds build/librocket.dll first — it is missing on this machine).
- Manual acceptance checklist (RESEARCH table): GUI-05 — orbit height / payload mass / stage count editable in Setup; NO ΔV input anywhere (visual + `rg "dv_spin"`); GUI-06 — 3 modes selectable, mode 3 reveals the box, modes 1/2 hide it; GUI-07 — diameter value persists in the widget and is readable from MainWindow state.
- Regression: Run + Save still work after the dv_spin removal (export header shows the computed ΔV).
- Phase gate: `python SRC/test_call.py` smoke (bridge unchanged — gui.py:80-110 untouched).

<success_criteria>
Mission inputs complete (orbit height 100-2000 km; payload/stages verbatim; read-only auto-ΔV label; zero ΔV inputs); Vehicle Configuration tab delivers modes 1/2/3 with correct visibility/persistence semantics and Phase 2-ready state; app fully runnable and export consistent after dv_spin removal.
</success_criteria>

<output>
Create `.planning/phases/01-centralized-3-tab-gui/01-02-SUMMARY.md` when done.
</output>

## Must-Haves (truths & prohibitions)

Truths (each explicitly verifiable):
1. Mission grid rows: 0 Orbit Height (100.0-2000.0, decimals 1, step 10.0, default 500.0); 1 payload verbatim; 2 stages verbatim; 3 read-only "ΔV (auto)" {:.2f} km/s label (TEXT_SEC 12px).
2. `rg "dv_spin" SRC/gui/gui.py` matches nothing — no ΔV input anywhere (D-10, GUI-05).
3. Auto-ΔV value feeds the label, run_staging(delta_v=...), and export header/filename; updates on orbit-height change.
4. _auto_delta_v() mirrors Orbit_calc.f90:9-10 (g_0=9.80665, Radius=6378.0), documented interim for Phase 2 (PIPE-01); no other physics duplication.
5. Vehicle tab: 3 mutually-exclusive radios with helper lines; mode 1 default checked; stored ints 1/2/3.
6. Diameter box (0.5-20.0, decimals 2, step 0.1, default 2.00) visible only in mode 3; value persists across toggles and tab switches.
7. Mode int + diameter value stored as MainWindow state; NOT wired into run_staging (Pitfall 8).

Prohibitions (descriptor-less, flagged-unverified):
- No ΔV input of any kind after this plan; the removed dv_spin must not reappear.
- GUI does not read config.txt (D-14); no Python physics reimplementation beyond the marked-interim _auto_delta_v one-liner (PIPE-02).
- Orbit height and diameter stay widget-bounded (100-2000 km; 0.5-20 m); no free-text input; no eval/exec (ASVS V5).
- Mode semantics map exactly to Fortran 1/2/3; mode 1 default checked (config.txt Diameter_setup = 1).
- Diameter box visibility contract holds and value persists — widgets never rebuilt.

## Edge Probe Assumptions (spec-less fallback — ALL 13 items flagged)

| Probe item | Status | Disposition |
|---|---|---|
| GUI-05 boundary | COVERED-BY-TRUTH #1 | FLAGGED-ASSUMPTION: Qt spinbox clamp semantics — orbit height 100.0-2000.0, payload 1.0-1,000,000, stages 1-3; one step either side clamps, never wraps/rejects. |
| GUI-05 precision | COVERED-BY-TRUTH #3 | FLAGGED-ASSUMPTION: ΔV display contract is f-string {:.2f} km/s (Python float formatting, no custom tie-break); engineering display precision only. |
| GUI-04 precision (ΔV label) | COVERED-BY-TRUTH #3 | Same {:.2f} contract on the read-only label; no custom rounding logic. |
| GUI-06 unclassified (mode switch) | COVERED-BY-TRUTH #5 | FLAGGED-ASSUMPTION: switch review — radios chosen over a combo so all three modes are visible and reveal/hide is explicit (UI-SPEC:179); exclusivity via QButtonGroup. |
| GUI-07 unclassified (input box) | COVERED-BY-TRUTH #6 | FLAGGED-ASSUMPTION: box behavior = visible iff mode 3; value persists (never rebuilt); range 0.5-20.0 m. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI numeric inputs -> solver | New orbit-height spinbox and diameter spinbox; typed values cross into the ctypes call and export filename |
| GUI state -> Phase 2 | Stored diameter_mode int 1/2/3 + diameter value are handoff state, not wired to Fortran this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | orbit_height / diameter_spin bounds | low | mitigate | Ranges enforced at construction (100-2000 km; 0.5-20 m; ASVS V5); GUI has zero free-text numeric inputs; no eval/exec of user data anywhere |
| T-02-02 | Tampering | _auto_delta_v() interim physics mirror | low | accept | One-line V_circ duplicate of Orbit_calc.f90:9-10 with constants cited from Typical_Data.f90:3-5; documented interim, removed at PIPE-01 (Phase 2); drift risk bounded by the docstring marker |
| T-02-03 | Tampering | ctypes DLL load (gui.py:57) | medium | accept | FIX-03 (Phase 3) removes the hardcoded MinGW resolution; Phase 1 does not touch gui.py:22-59 (diff guard) |
| T-02-SC | Tampering | package installs | low | accept | No new packages; PyQt6 6.11.0 already installed; Package Legitimacy Audit N/A |
</threat_model>