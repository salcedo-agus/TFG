---
phase: 01-centralized-3-tab-gui
plan: 3
slug: centralized-3-tab-gui
summary: Extend ResultCard with per-stage ΔV and geometry rows (placeholders pending Phase 2), add the partial-state hint and the auto-switch to Results after Run, and verify the retained export + minimum-confirmed indicator contracts (idempotent Save, blocking single-thread guarantee).
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_created: []
files_modified:
  - SRC/gui/gui.py
autonomous: true
requirements:
  - GUI-02
  - GUI-03
  - GUI-04
phase_req_ids:
  - GUI-02
  - GUI-03
  - GUI-04
user_setup: []
estimate:
  tokens: 35000
  raw_tokens: 21000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-03, D-04: After Run, each ResultCard shows the 8 existing metrics (m0, mf, mp, ms, k_m, k_s, k_L, nu_e) populated from the bridge dict PLUS 2 new grid rows: row 4 'Stage ΔV — km/s' + 'Diameter — m'; row 5 'Length — m' + 'Volume — m³' (UI-SPEC:144-155)."
    - "Formatting contract enforced (UI-SPEC:155): masses %,.1f kg; ratios %.4f; nu_e %.4f km/s; ΔV %.2f km/s; diameter/length %.2f m; volume %.2f m³ — via an add_metric fmt parameter defaulting to the legacy :,.1f so rows 0-3 render byte-identical to today."
    - "D-05, D-06: Partial: missing per-stage ΔV and geometry values render '—' in TEXT_DIM (data.get() defaults — never fabricated numbers); when any '—' is rendered, the partial-state hint 'ΔV and geometry populate with full pipeline wiring (Phase 2).' appears once below the cards (11px TEXT_DIM); it does NOT appear in the pre-run empty state (no placeholders present)."
    - "Auto-switch: a successful Run auto-switches to tab 0 (self.tabs.setCurrentIndex(0)) after the render and Save-enable (UI-SPEC:193); an input-change invalidation does NOT force navigation."
    - "D-07: Export contract (GUI-04): Save Results writes the same 'ROCKET STAGING RESULTS' header block as today with the Delta-V line from _auto_delta_v (never hand-entered); filename pattern staging_{n}stage_dv{dv:.1f}_pl{pl}kg.txt; repeated Save re-opens the native dialog each time — the app never writes twice to the same path without explicit user confirmation and never appends (idempotent)."
    - "D-07: Minimum-confirmed indicator logic verbatim: '✔ Minimum confirmed' GREEN / '✘ Minimum not confirmed — check your inputs' ACCENT2 from results['minimum_found'] (gui.py:957-963 pattern, relocated)."
    - "Concurrency guarantee (single-threaded): the solver call remains blocking on the UI thread; during a run the UI cannot accept input, so a second Run or a concurrent Save is impossible — no re-entrancy, no threading machinery (A6)."
    - "Populated state after run: summary -> indicator -> divider -> n cards; Save enabled; _last_configs/_last_results captured (gui.py:973-983, relocated in 01-01-T3)."
  artifacts:
    - "SRC/gui/gui.py — ResultCard rows 4-5 with fmt-aware add_metric and TEXT_DIM placeholders"
    - "SRC/gui/gui.py — partial-state hint label in the Results render path"
    - "SRC/gui/gui.py — setCurrentIndex(0) auto-switch after successful run"
  key_links:
    - "ResultCard data.get() defaults <-> Phase 2 bridge dict keys (dv, diameter, length, volume) — signature-stable handoff"
    - "Render block -> results_layout (empty-state index 0, hint appended after cards)"
    - "print_btn.setEnabled(True) <-> successful run; setCurrentIndex(0) fires in the same success path"
  prohibitions:
    - "No fabricated ΔV/geometry numbers: until Phase 2 packs them, the cards render '—', never a Python-computed value (PIPE-02 — no physics reimplementation in Python)."
    - "The export file format must not silently change: content identical to today except the Delta-V line source (auto value, D-10); no extra columns, reordering, or encoding change."
    - "Minimum-confirmed indicator semantics must not change (same message strings, colors, and minimum_found source)."
    - "Save must write only to the QFileDialog-picked path; no auto-overwrite of unrelated files, no appends to an existing file without the dialog's explicit overwrite confirmation."
    - "The solver must remain a blocking UI-thread call (A6); no QThread worker may be introduced to satisfy the concurrency truth."
---

<objective>
Complete the results surface: per-stage ΔV and geometry fields (placeholder "—" until Phase 2), partial-state hint, auto-switch to Results after Run, and the retained export + minimum-confirmed contracts.

Purpose: Fulfill D-03/D-04 (per-stage masses + ratios and exhaust velocity), D-05 (per-stage ΔV), D-06 (geometry), D-07 (export + minimum indicator) — i.e. GUI-02, GUI-03, and GUI-04 — on top of the shell relocated in 01-01. Partial population is user-approved (CONTEXT discretion); placeholders must look intentional, not broken (Pitfall 5).
Output: ResultCard rows 4-5 with TEXT_DIM "—" placeholders and exact per-metric formatting; hint label; auto-switch; export + indicator verified against the pre-phase behavior.
</objective>

<execution_context>
@C:/Users/agusc/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/agusc/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-centralized-3-tab-gui/01-CONTEXT.md
@.planning/phases/01-centralized-3-tab-gui/01-RESEARCH.md
@.planning/phases/01-centralized-3-tab-gui/01-UI-SPEC.md
@.planning/phases/01-centralized-3-tab-gui/01-PATTERNS.md
@SRC/gui/gui.py

# Plan 3 of 3 — results surface + export/indicator contracts (GUI-02, GUI-03, GUI-04)
Depends on 01-01 (Results tab relocation) and 01-02 (auto-ΔV in _print_results). Data availability per RESEARCH matrix:160-166 — per-stage ΔV is computed but not packed by C_Interface; geometry is never stored — both render "—" until Phase 2. The bridge return dict (gui.py:101-110) remains the only data source.
</context>

<tasks>

<task type="auto">
  <name>01-03-T1: Extend ResultCard with ΔV + geometry rows (D-05, D-06, GUI-02, GUI-03)</name>
  <commit>feat(gui): add ΔV and geometry rows to result cards</commit>
  <files>SRC/gui/gui.py</files>
  <action>
    Per D-03/D-04 (masses + ratios/exhaust velocity surface), rows 0-3 must keep rendering byte-identical: extend the `add_metric` closure in `ResultCard` (gui.py:532-577) with a `fmt` parameter defaulting to the legacy ":,.1f" so existing rows 0-3 render byte-identical; value text becomes f-string formatting of the given fmt with the unit appended (PATTERNS §5). Handle missing values BEFORE formatting: a value of None (from `data.get(key)` default) renders the placeholder glyph "—" in TEXT_DIM, never a formatted number.
    Per D-05/D-06, add exactly 2 new grid rows (UI-SPEC:144-155), unchanged signature call style:
    - Row 4: "Stage ΔV" — data.get("dv") — fmt "%.2f" — unit "km/s"; "Diameter" — data.get("diameter") — fmt "%.2f" — unit "m".
    - Row 5: "Length" — data.get("length") — fmt "%.2f" — unit "m"; "Volume" — data.get("volume") — fmt "%.2f" — unit "m³".
    Colors: ΔV/diameter/length/volume use TEXT_PRI when present, TEXT_DIM for the "—" placeholder (partial population, user-approved; UI-SPEC:155).
    Do NOT change the caller `ResultCard(stage["stage"], stage)` (gui.py:970) — the .get() defaults make Phase 2 dict keys (dv, diameter, length, volume) fill in without signature churn (PATTERNS §5 caller note).
  </action>
  <verify>
    <automated>python -m py_compile SRC/gui/gui.py</automated>
    <human-check>`make gui` in SRC/: run with any valid combo -> each card shows the 8 original metrics unchanged (values + formats identical to pre-phase) and 2 new rows all rendering "—" in dim text; no KeyError/ValueError on missing keys; window resizes without layout break.</human-check>
  </verify>
  <done>ResultCard shows 10 metric slots with exact per-metric formatting; ΔV/geometry render "—" TEXT_DIM (never fabricated values); existing 8 metrics byte-identical in format.</done>
  <reversibility rating="reversible">Additive grid rows with .get() defaults — revert restores the 8-row card; no behavior change to existing rows (fmt default preserves legacy rendering).</reversibility>
  <dependencies>01-01-T3 (render block + cards in Results tab).</dependencies>
</task>

<task type="auto">
  <name>01-03-T2: Partial-state hint, auto-switch, export/indicator contracts (D-07, GUI-02/03/04)</name>
  <commit>feat(gui): auto-switch to Results and add partial-state hint</commit>
  <files>SRC/gui/gui.py</files>
  <action>
    In the `_run` success path (render block relocated by 01-01-T3, ex-gui.py:945-985): after `print_btn.setEnabled(True)` add `self.tabs.setCurrentIndex(0)` so a successful Run auto-switches to the Results tab (UI-SPEC:193). Do NOT navigate on invalidation (_on_inputs_changed stays navigation-free).
    Partial-state hint: in the same render path, when ANY rendered card value is missing (i.e. any of dv/diameter/length/volume resolved to None), append ONE 11px TEXT_DIM QLabel with copy "ΔV and geometry populate with full pipeline wiring (Phase 2)." after the cards (UI-SPEC:112). It appears once per render (the render clears and rebuilds); it does NOT appear in the pre-run empty state (no placeholders present — only cards render placeholders).
    Export contract per D-07 (verify, do not redesign — _print_results was adapted in 01-02-T1): "ROCKET STAGING RESULTS" header block; Delta-V line from _auto_delta_v (never hand-entered, D-10); filename staging_{n}stage_dv{dv:.1f}_pl{pl}kg.txt; content otherwise identical to today's format. Minimum indicator (ex-gui.py:957-963) unchanged: GREEN "✔ Minimum confirmed" / ACCENT2 "✘ Minimum not confirmed — check your inputs", driven by results["minimum_found"].
    No QThread wiring: the solver call stays a blocking UI-thread call (A6) — this is the documented concurrency guarantee (single-threaded, no re-entrant runs possible).
  </action>
  <verify>
    <automated>python -m py_compile SRC/gui/gui.py</automated>
    <human-check>`make gui` in SRC/: Run -> app switches to Results automatically; hint appears once below the cards; changing an input invalidates (Results cleared, Save disabled) without forcing a tab change. Save twice -> the dialog opens each time; picking a NEW file or confirming overwrite yields a .txt identical in structure to today's export (compare with a pre-phase file: only the Delta-V line source differs). Minimum indicator shows ✔ for a confirmed run and ✘ otherwise (bridge flag). During the run the window is unresponsive until completion (blocking, expected).</human-check>
  </verify>
  <done>Successful Run lands the user on Results with populated cards + one hint; export and minimum indicator match the retained contracts; solver remains blocking (no threading machinery).</done>
  <reversibility rating="reversible">Additive UX behavior (auto-switch + hint); export/min-indicator logic untouched. Gate: export file structure must remain identical to today's — any deviation found during verification blocks the commit.</reversibility>
  <dependencies>01-03-T1 (cards render placeholders the hint refers to) and 01-02-T1 (auto-ΔV in export).</dependencies>
</task>

</tasks>

## Verification (per RESEARCH "Validation Architecture")

- Per-task commit gate: `python -m py_compile SRC/gui/gui.py`.
- GUI smoke via `make gui` in `SRC/` (builds build/librocket.dll first — missing on this machine).
- Manual acceptance checklist (RESEARCH table): GUI-02 — per-stage m0/mf/mp/ms/k_m/k_s/k_L/nu_e populated; ΔV field present as "—"; GUI-03 — diameter/length/volume fields present as "—"; GUI-04 — "✔ Minimum confirmed" logic unchanged; Save writes a .txt with the same content as today minus the removed ΔV input line (Delta-V now auto).
- Idempotency check: run twice with identical inputs, save twice, diff the two files — content must be identical (same header, values, filename pattern).
- Phase gate: `python SRC/test_call.py` smoke (bridge unchanged) + full acceptance walkthrough of all 8 GUI requirements across the three plans.

<success_criteria>
Results tab is the full required surface: 10 metric slots per stage (8 populated, ΔV + 3 geometry placeholders), exact formatting contract, partial-state hint, auto-switch after Run, idempotent Save export with auto-ΔV line, verbatim minimum indicator, blocking single-thread guarantee — all verified via the manual checklist on `make gui`.
</success_criteria>

<output>
Create `.planning/phases/01-centralized-3-tab-gui/01-03-SUMMARY.md` when done.
</output>

## Must-Haves (truths & prohibitions)

Truths (each explicitly verifiable):
1. ResultCard: 8 existing metrics + 2 new rows (Stage ΔV/Diameter; Length/Volume) per UI-SPEC:144-155.
2. Formatting contract: masses %,.1f; ratios %.4f; nu_e %.4f; ΔV %.2f; geometry %.2f — fmt-aware add_metric with legacy default (rows 0-3 byte-identical).
3. Partial: missing values render "—" TEXT_DIM via .get() defaults; hint "ΔV and geometry populate with full pipeline wiring (Phase 2)." once below cards; not in pre-run empty state.
4. Auto-switch to tab 0 after successful Run; invalidation does not navigate.
5. Export: same header block; Delta-V from _auto_delta_v; filename pattern kept; repeated Save re-opens the dialog (no silent double-write, no append).
6. Minimum indicator verbatim (messages, colors, minimum_found source).
7. Concurrency: blocking solver on UI thread; no re-entrancy possible; no threading machinery (A6).
8. Populated state: summary -> indicator -> divider -> n cards; Save enabled; _last_configs/_last_results captured.

Prohibitions (descriptor-less, flagged-unverified):
- No fabricated ΔV/geometry numbers in the cards before Phase 2 (PIPE-02).
- Export format does not silently change (only the Delta-V line source differs from today).
- Minimum-confirmed indicator semantics unchanged.
- Save writes only to the dialog-picked path; no append/overwrite without explicit confirmation.
- Solver stays blocking; no QThread worker introduced.

## Edge Probe Assumptions (spec-less fallback — ALL 13 items flagged)

| Probe item | Status | Disposition |
|---|---|---|
| GUI-02 unclassified (metrics display) | COVERED-BY-TRUTH #1 | FLAGGED-ASSUMPTION: display review — 8 legacy metrics keep legacy formatting; new rows follow the locked formatting contract; card grid pattern retained (Open Question 4 resolved: cards, not table). |
| GUI-03 empty (geometry lists) | COVERED-BY-TRUTH #3 | FLAGGED-ASSUMPTION: no list-style geometry input exists; the surface is per-stage fields rendering "—" until Phase 2 — single-element/null-input semantics not applicable. |
| GUI-03 encoding | FLAGGED-ASSUMPTION | Not applicable: geometry values are numeric (m, m³); byte/codepoint/grapheme/normalized-form equality definitions do not apply. |
| GUI-04 boundary (one step either side) | COVERED-BY-TRUTH #5/#7 | FLAGGED-ASSUMPTION: Qt clamp semantics at every bound (stages 1-3, orbit 100-2000, payload 1-1e6, diameter 0.5-20); no wrap/reject; verified visually at bounds during the checklist. |
| GUI-04 precision (results formatting) | COVERED-BY-TRUTH #2 | FLAGGED-ASSUMPTION: fixed decimals per metric via f-string formats; Python float formatting, no custom tie-break; engineering display precision. |
| GUI-04 idempotency (Save twice) | COVERED-BY-TRUTH #5 | FLAGGED-ASSUMPTION: second Save re-opens the native dialog; user picks a new file or explicitly confirms overwrite; identical content between identical runs (verified by diff). |
| GUI-04 concurrency | COVERED-BY-TRUTH #7 | FLAGGED-ASSUMPTION: single-threaded app — the blocking call makes the UI unresponsive mid-run, which IS the guarantee: no second Run/Save can start concurrently; interruption is Ctrl+C at the console only. |
| GUI-05 precision (ΔV in cards) | COVERED-BY-TRUTH #3 | FLAGGED-ASSUMPTION: when Phase 2 populates "dv", the card renders %.2f km/s per the formatting contract; the "—" placeholder is the only Phase 1 state. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI -> filesystem (Save Results) | Export written to the QFileDialog-picked path; filename derives from validated numeric values |
| GUI display <- bridge dict | Untrusted-ish data source: cards format whatever run_staging returns (incl. garbage per FIX-01 caveat — display only, no Fortran edits) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | _print_results export path | low | mitigate | Writes only to the QFileDialog-picked path; default name from numeric spinbox values; no free-text path construction; idempotent (dialog re-opens per save, ASVS V5) |
| T-03-02 | Denial of Service | blocking solver call | low | accept | Deliberate (A6): fast solver, single-user desktop; no spinner/skeleton exists or is intended (UI-SPEC loading row) |
| T-03-03 | Tampering | placeholder integrity | low | mitigate | "—" placeholders via data.get() defaults; no Python-side ΔV/geometry computation (PIPE-02); prevents fabricated results from being mistaken for physics output |
| T-03-04 | Tampering | ctypes DLL load (gui.py:57) | medium | accept | FIX-03 (Phase 3) — guard note only; Phase 1 diff guard keeps gui.py:22-59 untouched |
| T-03-SC | Tampering | package installs | low | accept | No new packages; PyQt6 already installed; Package Legitimacy Audit N/A |
</threat_model>