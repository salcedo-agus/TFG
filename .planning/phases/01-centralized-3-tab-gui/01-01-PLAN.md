---
phase: 01-centralized-3-tab-gui
plan: 1
slug: centralized-3-tab-gui
summary: Build the QTabWidget shell (Results / Setup / Vehicle Configuration), extend STYLE with tab-bar + radio QSS, and relocate the existing input panel and results panel verbatim into the Setup and Results tabs. Splash and AppWindow untouched (GUI-08).
type: execute
wave: 1
depends_on: []
files_created: []
files_modified:
  - SRC/gui/gui.py
autonomous: true
requirements:
  - GUI-01
  - GUI-04
  - GUI-08
phase_req_ids:
  - GUI-01
  - GUI-04
  - GUI-08
user_setup: []
estimate:
  tokens: 50000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "App opens to exactly 3 tabs in contractual order: Results (index 0, active on open), Setup, Vehicle Configuration — labels exact (UI-SPEC:129-137); tabs not movable/closable/renamable (QTabWidget defaults untouched)."
    - "Tab bar + radio styling applied by appending one QSS block to STYLE (UI-SPEC:88-98) using only the 12 named palette constants (gui.py:134-146); existing STYLE rules untouched (append-only); selected tab ACCENT #58a6ff."
    - "Every tab page is a QScrollArea with setWidgetResizable(True) and stretch bookends (existing container pattern gui.py:720-727, 803-815); setMinimumSize(960, 700) preserved on MainWindow (gui.py:708) and AppWindow (gui.py:994)."
    - "Setup tab shows the relocated header, mission group, stage input widgets, and Run button verbatim (D-08): no widget redesign; _rebuild_stage_inputs rebuilds exactly n stage widgets for n_stages_spin 1-3; no zero-stage state exists."
    - "Run button ready=false renders dim BG_INPUT via setProperty('ready') + unpolish/polish (gui.py:787-793, 853-857); print_btn setEnabled(False) until first successful run (gui.py:786)."
    - "D-07: Results tab before first run renders 'Run the analysis to see results here.' at layout index 0 (existing _show_empty_state pattern gui.py:840-845, relocated); after Run: summary -> minimum indicator -> divider -> n ResultCards; Save enabled; _last_configs/_last_results captured (gui.py:973-983)."
    - "Error modals preserved: 'Missing Data' warning with stage list + guidance (gui.py:924-931) and 'Fortran Error' critical with exception text + recovery path (gui.py:933-943)."
    - "Splash preserved (GUI-08): AppWindow QStackedWidget + 600 ms InOutQuad fade (gui.py:990-1019) untouched; splash still shows first on launch."
    - "{ statement: 'Combo boxes get setMinimumWidth(220) + Qt elision; dropdown remains readable and the tab bar fits at 960px min width', verification: backstop }"
  artifacts:
    - "SRC/gui/gui.py — QTabWidget central widget with three tab pages (indexes 0/1/2)"
    - "SRC/gui/gui.py — Setup tab page containing relocated header/mission/stages_container/run_btn"
    - "SRC/gui/gui.py — Results tab page containing relocated render surface + print_btn"
  key_links:
    - "validity_changed (StageInputWidget) -> _update_run_button + _on_inputs_changed fan-out intact across tab relocation"
    - "_rebuild_stage_inputs operates on the Setup tab's stages_layout"
    - "_print_results reads _last_configs/_last_results captured in the Results render path"
  prohibitions:
    - "The old root_layout QHBox split (gui.py:713-717) must be fully replaced by the tab shell, not kept alongside it — no ghost/duplicate panels."
    - "Tab labels stay exact English strings; tabs must not be made movable, closable, or renamable."
    - "No StageInputWidget redesign during relocation (D-08): sliders, combos, labels, and validity logic move verbatim."
    - "AppWindow, SplashScreen, and the 600 ms InOutQuad fade must not be modified (D-13)."
    - "The solver call must remain blocking on the UI thread; QThread must not be wired to _run (A6)."
---

<objective>
Build the 3-tab shell and relocate the two existing panels into it without changing their behavior.

Purpose: Establish the tabbed architecture (D-01, D-02) that the rest of the phase builds on, proving the shell end-to-end before the mission-input rework (01-02) and the results surface extension (01-03) land.
Output: A runnable `SRC/gui/gui.py` where the app opens to a styled 3-tab workbench; Setup contains the verbatim input panel (sliders, selectors, Run); Results contains the verbatim render surface (summary, minimum indicator, cards, Save Results); splash unchanged.
</objective>

<execution_context>
@C:/Users/agusc/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/agusc/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-centralized-3-tab-gui/01-CONTEXT.md
@.planning/phases/01-centralized-3-tab-gui/01-RESEARCH.md
@.planning/phases/01-centralized-3-tab-gui/01-UI-SPEC.md
@.planning/phases/01-centralized-3-tab-gui/01-PATTERNS.md
@SRC/gui/gui.py

# Plan 1 of 3 — shell + verbatim relocation (GUI-01, GUI-04, GUI-08)
Single-file restructuring of `SRC/gui/gui.py` per PATTERNS.md. All line refs below are verified anchors. The mission-input rework (dv_spin removal, orbit height) is deliberately NOT in this plan — it lands in 01-02-T1. `dv_spin` relocates verbatim here and stays functional until then.
</context>

<tasks>

<task type="tracer">
  <name>01-01-T1: Add QTabWidget shell and tab-bar/radio QSS</name>
  <commit>feat(gui): add 3-tab QTabWidget shell with styled tab bar</commit>
  <files>SRC/gui/gui.py</files>
  <action>
    Add `QTabWidget` and `QRadioButton` to the PyQt6.QtWidgets import list (gui.py:9-15) — keep stdlib → PyQt6 → local order (PATTERNS §1). Keep the dead `QThread` import (gui.py:16) — solver stays blocking (A6).
    Append to the `STYLE` f-string (gui.py:147-312), BEFORE the closing delimiter, exactly the QSS block from UI-SPEC:88-98 (QTabWidget::pane, QTabBar::tab, QTabBar::tab:selected, QTabBar::tab:!selected:hover, QTabBar::tab:top:!selected, QRadioButton + indicator rules). Use only the 12 named palette constants from gui.py:134-146 via f-string interpolation. Do NOT touch any existing STYLE rule (append-only; PATTERNS §6).
    In `_build_ui` (gui.py:712), replace the root_layout QHBox construction (gui.py:713-717) with: `self.tabs = QTabWidget()`; `self.setCentralWidget(self.tabs)`; then addTab in contractual order: "Results" (index 0), "Setup" (index 1), "Vehicle Configuration" (index 2) (D-02; UI-SPEC:129-137). Implement three builders `_build_results_tab()`, `_build_setup_tab()`, `_build_vehicle_tab()`, each returning a QScrollArea page: QScrollArea + setWidgetResizable(True) + inner QWidget + QVBoxLayout, margins 16/20/16/20 for Setup and 24/24/24/24 for Results (UI-SPEC lg token), addStretch() bookends (PATTERNS §2, gui.py:720-727, 803-815). At this commit the three pages are empty containers; the left/right panel construction code in `_build_ui` stays in place (widgets created but orphaned — harmless intermediate state; relocation lands in T2/T3).
    Keep `setMinimumSize(960, 700)` on MainWindow (gui.py:708) and AppWindow (gui.py:994) untouched.
  </action>
  <verify>
    <automated>python -m py_compile SRC/gui/gui.py</automated>
    <human-check>`make gui` in SRC/ (builds build/librocket.dll then launches): splash shows first (GUI-08), LAUNCH fades into a window with 3 styled tabs — Results active, then Setup, then Vehicle Configuration; tab bar renders dark-theme (selected tab ACCENT), window enforces 960x700 minimum; no trace of the old left/right split visible.</human-check>
  </verify>
  <done>App launches to a styled 3-tab workbench in contractual order; splash untouched; py_compile passes; existing STYLE rules byte-identical (append-only).</done>
  <reversibility rating="reversible">Pure additive shell — git revert restores the split layout with zero data loss; no widget state touched.</reversibility>
  <dependencies>None (Wave 1).</dependencies>
</task>

<task type="auto">
  <name>01-01-T2: Relocate input panel into Setup tab (verbatim, D-08)</name>
  <commit>refactor(gui): move input panel into Setup tab verbatim</commit>
  <files>SRC/gui/gui.py</files>
  <action>
    Move the left-panel construction code from `_build_ui` into `_build_setup_tab`, in this exact order (PATTERNS §3): header title/subtitle/divider (gui.py:732-739); mission group with `pl_spin`, `n_stages_spin` AND `dv_spin` verbatim (gui.py:742-768 — dv_spin stays for this plan; removal is 01-02-T1, do NOT remove here); `stages_container`/`stages_layout` (gui.py:772-777); `run_btn` with ready-property QSS (gui.py:780-795). Widgets move by reference — do NOT rebuild or restyle anything (D-08 gate: slider/selector widget state must survive relocation; StageInputWidget class gui.py:319-528 untouched).
    Preserve the signal wiring verbatim (gui.py:817-819, 833-834): `pl_spin.valueChanged` -> `_on_inputs_changed`; `n_stages_spin.valueChanged` -> `_on_inputs_changed` and `_rebuild_stage_inputs`; per StageInputWidget: `validity_changed` -> `_update_run_button` and `_on_inputs_changed`. `_rebuild_stage_inputs` (gui.py:825-838) now operates on the Setup tab's `stages_layout` (it already references the container attribute — confirm the attribute name still resolves post-relocation).
    Backstop (UI-SPEC row "long-text"): in `StageInputWidget._build` (gui.py:331-347), call `setMinimumWidth(220)` on both NoScrollComboBox instances (propellant and cycle combos) so the 61-char cycle name remains readable via Qt elision; no other combo changes.
  </action>
  <verify>
    <automated>python -m py_compile SRC/gui/gui.py</automated>
    <human-check>`make gui` in SRC/: Setup tab shows header, MISSION PARAMETERS (payload, stages, ΔV spinbox), stage widgets with sliders, and Run button exactly as before; changing stage count rebuilds exactly n widgets with no layout break; Run button ready-state toggles with validity; combos elide the long cycle name readably at 960px width; tab bar still fits.</human-check>
  </verify>
  <done>Setup tab is a verbatim copy of the old input panel (zero redesign); all signal connections functional across the tab boundary; combos at least 220px wide.</done>
  <reversibility rating="reversible">Relocation only — git revert restores the split; gate note: do NOT recreate StageInputWidgets during the move (state loss) — move construction code, not instances.</reversibility>
  <dependencies>01-01-T1 (shell must exist).</dependencies>
</task>

<task type="auto">
  <name>01-01-T3: Relocate results panel into Results tab (verbatim, D-07, GUI-04)</name>
  <commit>refactor(gui): move results panel into Results tab verbatim</commit>
  <files>SRC/gui/gui.py</files>
  <action>
    Per D-07, the export + minimum-confirmed machinery is retained in the Results tab: move the right-panel construction (gui.py:803-815) into `_build_results_tab`, retargeting every `self.right_layout` reference to the Results tab layout (`self.results_layout`): in `_show_empty_state` (gui.py:840-845), `_clear_results` (gui.py:847-851), and the render block inside `_run` (gui.py:945-985). Relocate `print_btn` ("⬇ Save Results", gui.py:783-786) into the Results tab, pinned above the final addStretch() (UI-SPEC:156); keep its initial `setEnabled(False)` (gui.py:786) and the enable-after-run logic (gui.py:981).
    Preserve the render block verbatim in this order (UI-SPEC:141-157): summary rich-text label (gui.py:948-955), minimum indicator `min_label` (gui.py:957-963 — GREEN "✔ Minimum confirmed" / ACCENT2 "✘ Minimum not confirmed — check your inputs", logic unchanged), divider QFrame, ResultCard loop (gui.py:969-971), `_last_configs`/`_last_results` capture (gui.py:973-983 — leave the harmless duplicated assignment at gui.py:980/983 as-is, RESEARCH:338), `print_btn.setEnabled(True)`. `_on_inputs_changed` (gui.py:859-862) keeps clearing results + disabling Save — it calls `_clear_results()` which now targets the Results tab (Pitfall 3: cross-tab invalidation must survive).
    Do NOT add the auto-switch to tab 0 yet (lands in 01-03-T2) and do NOT add the partial-state hint (lands in 01-03-T2).
  </action>
  <verify>
    <automated>python -m py_compile SRC/gui/gui.py</automated>
    <human-check>`make gui` in SRC/: from Setup, click Run with valid combos -> results appear in the Results tab (summary, indicator, divider, cards); Save Results writes a .txt with the CURRENT format (incl. Delta-V from the spinbox, unchanged in this plan); changing any Setup input clears Results and disables Save; empty state copy shows before first run at layout index 0.</human-check>
  </verify>
  <done>Results tab reproduces the old right panel exactly: empty state, render order, Save export (identical file content to today), minimum indicator, capture of _last_configs/_last_results; cross-tab invalidation works.</done>
  <reversibility rating="reversible">Relocation only — revert restores the split; gate note: export must still write an identical file to today's format (compare a saved file before/after via git stash if in doubt).</reversibility>
  <dependencies>01-01-T2 (Setup tab complete; Run must produce results).</dependencies>
</task>

</tasks>

## Verification (per RESEARCH "Validation Architecture")

- Per-task commit gate: `python -m py_compile SRC/gui/gui.py` (syntax, < 5 s).
- GUI smoke requires `build/librocket.dll` (missing on this machine — RESEARCH:304-310): every GUI run starts with `make gui` in `SRC/` (builds the DLL and launches) or `make all` then `python SRC/gui/gui.py`.
- Manual acceptance checklist (RESEARCH table): GUI-08 splash first + fade; GUI-01 three tabs, Results active; Setup sliders/selectors/Run verbatim; Results empty state -> render -> Save.
- Phase gate: `python SRC/test_call.py` smoke (bridge behavior unchanged — we did not touch gui.py:80-110).
- Diff guard: `git diff SRC/gui/gui.py` must show zero changes in the ranges gui.py:22-59 (DLL load), 80-110 (bridge), 114-130 (_load_data), 582-700 (SplashScreen), 990-1019 (AppWindow/fade), 1023-1028 (entry point).

<success_criteria>
The app opens to a styled, ordered 3-tab workbench; Setup and Results tabs reproduce the old panels verbatim (widgets, wiring, export, indicator, empty state); splash untouched; `make gui` runs green; a saved export is byte-identical to the pre-refactor format.
</success_criteria>

<output>
Create `.planning/phases/01-centralized-3-tab-gui/01-01-SUMMARY.md` when done.
</output>

## Must-Haves (truths & prohibitions)

Truths (each explicitly verifiable):
1. Three tabs, contractual order and labels, Results index 0 active on open; tabs not movable/closable/renamable.
2. Tab-bar/radio QSS appended to STYLE using only the 12 palette constants; existing rules untouched.
3. Every tab page a QScrollArea with setWidgetResizable(True) + stretch bookends; setMinimumSize(960, 700) preserved on MainWindow and AppWindow.
4. Setup tab: verbatim relocation (D-08); _rebuild_stage_inputs rebuilds exactly n widgets for 1-3 stages; no zero-stage state.
5. run_btn ready=false -> dim BG_INPUT via unpolish/polish; print_btn disabled until first successful run.
6. Results empty state "Run the analysis to see results here." at layout index 0; after Run: summary -> indicator -> divider -> n cards; Save enabled; _last_configs/_last_results captured.
7. "Missing Data" and "Fortran Error" modals preserved.
8. Splash preserved (GUI-08): AppWindow stack + 600 ms InOutQuad fade untouched; splash shows first.
9. Backstop marker: `{ statement: "Combo boxes get setMinimumWidth(220) + Qt elision; dropdown remains readable and the tab bar fits at 960px min width", verification: backstop }` — verifier abstains -> human_needed if unconfirmed.

Prohibitions (descriptor-less, flagged-unverified):
- The old QHBox split must be fully replaced, not kept alongside the tabs (no ghost panels).
- Tab labels stay exact; no movable/closable/renamable tabs.
- No StageInputWidget redesign during relocation (D-08).
- AppWindow/SplashScreen/600 ms InOutQuad fade not modified (D-13).
- Solver stays a blocking UI-thread call; QThread not wired to _run (A6).
- dv_spin is NOT removed in this plan (belongs to 01-02-T1) — premature removal would break _print_results (gui.py:869, 885) and _run (gui.py:936).

## Edge Probe Assumptions (spec-less fallback — ALL 13 items flagged)

| Probe item | Status | Disposition |
|---|---|---|
| GUI-01 unclassified (tab structure) | COVERED-BY-TRUTH #1 | Tab order/labels/lockdown are the acceptance contract; FLAGGED-ASSUMPTION: tab structure is fixed at 3 pages, no user reordering. |
| GUI-04 boundary (stage count) | COVERED-BY-TRUTH #4 | FLAGGED-ASSUMPTION: Qt spinbox clamps at bounds — incrementing 3 stays 3; decrementing 1 stays 1; one step either side is clamped, never wrapped/rejected. |
| GUI-08 unclassified (splash) | COVERED-BY-TRUTH #8 | FLAGGED-ASSUMPTION: splash sequence, 600 ms InOutQuad fade, and LAUNCH behavior verified by diff-guard (no edits to gui.py:582-700, 990-1019). |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI -> filesystem (Save Results) | User picks a path via native dialog; the app writes a text export to that exact path (relocated print_btn) |
| GUI -> librocket.dll (ctypes) | DLL loaded at import from build/ (gui.py:57); MinGW path resolution hardcoded (FIX-03 -> Phase 3) |
| GUI numeric inputs | Spinboxes only; no free-text entry exists in the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | _print_results export path | low | mitigate | Writes only to the path returned by QFileDialog.getSaveFileName; default filename built from validated numeric spinbox values only; no free-text path input exists anywhere in the GUI (ASVS V5) |
| T-01-02 | Tampering | ctypes DLL load (gui.py:57) | medium | accept | Hardcoded MinGW search + DLL load are FIX-03 (Phase 3); Phase 1 must NOT modify gui.py:22-59. Guard note: future phase validates DLL provenance before load |
| T-01-03 | Denial of Service | blocking solver call (_run) | low | accept | Solver is fast (bisection, <=50 iterations); blocking is deliberate (A6); no QThread wiring this phase; single-user local app |
| T-01-SC | Tampering | package installs | low | accept | No new packages — PyQt6 6.11.0 already installed; Package Legitimacy Audit N/A (RESEARCH:289) |
</threat_model>