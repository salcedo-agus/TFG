---
phase: 01-centralized-3-tab-gui
plan: 5
slug: centralized-3-tab-gui
summary: Restore the test_call.py bridge-smoke import after the 72c468f interface/ reorg by mirroring the gui.py:29 sys.path pattern, and correct the stale TESTING.md run instruction. Closes G-01-4 (ModuleNotFoundError blocker on `python SRC/test_call.py`).
type: execute
wave: 1
depends_on: []
files_created: []
files_modified:
  - SRC/test_call.py
  - .planning/codebase/TESTING.md
autonomous: true
user_setup: []
estimate:
  tokens: 12000
  raw_tokens: 8000
  tasks: 2
  confidence: low
gap_closure: true
gap_ids: [G-01-4]
must_haves:
  truths:
    - "`python SRC/test_call.py` resolves `from rocket_lib import run_staging` from ANY working directory (repo root and SRC/ both verified) — ModuleNotFoundError is gone; the only possible remaining failure is the documented machine blocker (build/librocket.dll absent), which surfaces as a DLL-load error (OSError/FileNotFoundError naming librocket.dll), NOT an import error."
    - "Bridge behavior unchanged by construction: SRC/interface/rocket_lib.py and SRC/gui/gui.py bridge code (gui.py:22-110) have zero diff — the fix is confined to test_call.py path setup."
    - "TESTING.md:17 run instruction describes the post-reorg layout (rocket_lib.py lives in interface/, runs from any CWD); no 'must be run from SRC/' wording remains."
    - "Full gate pass (finite masses printed) is achieved once build/librocket.dll exists — completed by 01-04's `make all` rebuild (wave 2); on a machine with the DLL present, this plan alone passes the gate."
  artifacts:
    - "SRC/test_call.py — sys.path.insert(0, <SRC>/interface) mirroring gui.py:29 before the bare import"
    - ".planning/codebase/TESTING.md — corrected Run Commands line"
  key_links:
    - "test_call.py sys.path setup -> SRC/interface/rocket_lib.py (the moved module; import must resolve from any CWD)"
    - "rocket_lib.py -> build/librocket.dll (module-level ctypes.CDLL at rocket_lib.py:22) — the machine blocker boundary for the full gate pass"
  prohibitions:
    - "Do NOT rewrite the import to `interface.rocket_lib` — keep the bare `from rocket_lib import run_staging` and fix the path, exactly mirroring gui.py:29 (the surviving in-repo pattern)."
    - "Do NOT alter the hardcoded 3-stage case (test_call.py:4-10) or the print statements (12-17) — the smoke case is a contract."
    - "Do NOT modify SRC/interface/rocket_lib.py or SRC/gui/gui.py (zero-diff requirement on the bridge, gui.py:22-110)."
    - "Do NOT add a task that builds build/librocket.dll — its absence is a documented machine blocker (STATE.md); the rebuild belongs to 01-04's verification."
    - "Do NOT touch other lines of TESTING.md beyond the Run Commands line (minimal doc correction)."
---

<objective>
Make the phase gate `python SRC/test_call.py` runnable again by fixing the stale bare import, and align the codebase-map documentation with the current layout.

Purpose: G-01-4 root cause is that commit 72c468f moved `SRC/rocket_lib.py` to `SRC/interface/rocket_lib.py` and added sys.path handling to gui.py (gui.py:29), but `test_call.py:2` still does a bare `from rocket_lib import run_staging` with no path setup — Python puts the script's own directory (SRC/) at sys.path[0], which no longer contains the module, so the import fails deterministically from any CWD (Bohrbug, latent since Jun 14). This plan mirrors the surviving gui.py pattern. Note: this is the import half of bridge-module usability (groundwork for FIX-02's consolidation, which stays in Phase 3 — this plan does NOT claim FIX-02 complete). Full gate pass still requires build/librocket.dll (documented machine blocker; 01-04's rebuild provides it).

Output: `SRC/test_call.py` imports resolve from any CWD; `TESTING.md` run instructions accurate; the UAT Test 4 gate passes on machines with the DLL present.
</objective>

<execution_context>
@C:/Users/agusc/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/agusc/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-centralized-3-tab-gui/01-UAT.md
@.planning/debug/DEBUG-test-call-import-error.md
@SRC/test_call.py
@SRC/interface/rocket_lib.py
@SRC/gui/gui.py
@.planning/codebase/TESTING.md

# Gap-closure plan 5 of phase 01 — G-01-4 (blocker): test_call.py bare import fails.
Evidence (DEBUG-test-call-import-error.md): only commit ac19992 ever touched test_call.py (byte-identical since creation, when rocket_lib.py was a sibling in SRC/); 72c468f is the introducing commit (moved rocket_lib.py → interface/, added sys.path handling to gui.py only); gui.py:23-29 is the surviving in-repo pattern (`GUI_DIR`/`ROOT_DIR` from `__file__`, then `sys.path.insert(0, os.path.join(ROOT_DIR, "interface"))`). TESTING.md:17 ("must be run from SRC/ (imports rocket_lib)") is stale post-reorg. rocket_lib.py loads the DLL at module level (rocket_lib.py:22), so the full gate pass additionally requires build/librocket.dll — documented machine blocker, do NOT build it in this plan.

Line anchors verified in this session: test_call.py:2 bare import; gui.py:23-29 path pattern.
</context>

<tasks>

<task type="auto">
  <name>01-05-T1: Add interface/ to sys.path in test_call.py before the import</name>
  <commit>fix(bridge): restore rocket_lib import in test_call.py after interface/ reorg</commit>
  <files>SRC/test_call.py</files>
  <action>
    Insert path setup at the top of `SRC/test_call.py`, BEFORE the existing `from rocket_lib import run_staging` (line 2), mirroring the gui.py:23-29 pattern exactly:
    - `import os` and `import sys` (stdlib imports first, matching repo convention).
    - `sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "interface"))` — resolves to SRC/interface/ regardless of the CWD the script is launched from.
    Keep the bare `from rocket_lib import run_staging` unchanged (do NOT rewrite to `interface.rocket_lib` — the bare name plus path fix is the exact gui.py:29 pattern and keeps the module's `__file__`-based DLL path resolution at rocket_lib.py:12-15 intact; `INTERFACE_DIR = os.path.dirname(os.path.abspath(__file__))` is how the module locates interface/ and build/).
    Do NOT touch the hardcoded 3-stage case (test_call.py:4-10) or the print statements (12-17) — the smoke case is a contract.
    Do NOT modify SRC/interface/rocket_lib.py or SRC/gui/gui.py (zero-diff requirement on the bridge, gui.py:22-110).
  </action>
  <verify>
    <automated>python -m py_compile SRC/test_call.py; then python SRC/test_call.py run from BOTH the repo root and from SRC/ (two CWDs) — in both cases the ModuleNotFoundError for rocket_lib must be GONE. Outcome is one of: (a) full pass printing Total initial mass + per-stage masses (build/librocket.dll present), or (b) failure at the documented DLL-load step with an OSError/FileNotFoundError naming librocket.dll (machine blocker — NOT an import error).</automated>
  </verify>
  <done>Import resolves deterministically from any CWD; the gate's only possible failure mode is the pre-existing DLL machine blocker, never the import.</done>
  <reversibility rating="reversible">Four added lines in a smoke script — revert-safe, no behavior surface beyond import resolution.</reversibility>
  <dependencies>None (Wave 1).</dependencies>
</task>

<task type="auto">
  <name>01-05-T2: Correct the stale TESTING.md run instruction</name>
  <commit>docs(testing): correct test_call.py run instruction for the interface/ layout</commit>
  <files>.planning/codebase/TESTING.md</files>
  <action>
    In `.planning/codebase/TESTING.md`, replace the Run Commands line 17 (`python test_call.py       # manual DLL smoke test — must be run from SRC/ (imports rocket_lib)`) with an accurate post-reorg description: the script imports rocket_lib from `interface/` (moved by the 72c468f reorg), runs from any working directory, and needs `build/librocket.dll` to pass (build it with `make all` in SRC/).
    Minimal edit: only the Run Commands line. Do NOT rewrite the test-case snippet at lines 40-45 (the bare `from rocket_lib import run_staging` shown there is accurate post-fix), do NOT touch the file organization or coverage sections.
  </action>
  <verify>
    <automated>(Select-String -Path .planning\codebase\TESTING.md -Pattern 'must be run from SRC/').Count -eq 0 — must hold (PowerShell-native, no grep on PATH); plus (Select-String -Path .planning\codebase\TESTING.md -Pattern 'interface/').Count -gt 0 and (Select-String -Path .planning\codebase\TESTING.md -Pattern 'build/librocket.dll').Count -gt 0 — the line 17 replacement text must mention both interface/ and build/librocket.dll.</automated>
  </verify>
  <done>Run instructions describe the current layout: interface/ import, any-CWD execution, DLL prerequisite.</done>
  <reversibility rating="reversible">Documentation-only line edit.</reversibility>
  <dependencies>01-05-T1 (doc must describe the post-fix behavior).</dependencies>
</task>

</tasks>

## Verification (G-01-4 closure gates)

- **Gate A — syntax (automated):** `python -m py_compile SRC/test_call.py`.
- **Gate B — import resolution (automated, two CWDs):** `python SRC/test_call.py` from repo root AND from `SRC/` — ModuleNotFoundError for rocket_lib must not occur. If `build/librocket.dll` is absent (documented machine blocker, STATE.md), the run fails at the DLL-load step (OSError naming librocket.dll) — record the gate as machine-blocked, NOT as a gap; the import defect is closed. If the DLL is present, the gate passes fully (finite masses printed).
- **Gate C — full pass (conditional):** after 01-04's `make all` rebuild (wave 2, Fortran fix), re-run `python SRC/test_call.py` — full pass with finite masses; this completes the UAT Test 4 truth "Bridge smoke passes".
- **Gate D — docs (automated):** PowerShell Select-String gate from T2 — no "must be run from SRC/" text remains in TESTING.md, and the replacement line mentions both `interface/` and `build/librocket.dll`.
- **Diff guard:** `git diff` on `SRC/gui/gui.py` and `SRC/interface/rocket_lib.py` must be zero (bridge behavior unchanged by construction).

<success_criteria>
UAT Test 4 truth restored: "Bridge smoke passes; behavior unchanged by construction (zero diff in gui.py:22-110)" — `python SRC/test_call.py` resolves the import from any CWD; the only remaining gate condition is the documented machine blocker (DLL presence), satisfied by 01-04's rebuild; TESTING.md describes the current layout.
</success_criteria>

<output>
Create `.planning/phases/01-centralized-3-tab-gui/01-05-SUMMARY.md` when done.
</output>

## Must-Haves (truths & prohibitions)

Truths (each explicitly verifiable):
1. `python SRC/test_call.py` from any CWD: no ModuleNotFoundError; worst-case failure is the documented DLL-load error, never the import.
2. Zero diff in `SRC/gui/gui.py` (bridge, gui.py:22-110) and `SRC/interface/rocket_lib.py`.
3. `TESTING.md:17` describes interface/ layout, any-CWD execution, and the DLL prerequisite; no "must be run from SRC/" text remains.
4. Full gate pass achievable once `build/librocket.dll` exists (provided by 01-04's rebuild).

Prohibitions (descriptor-less, flagged-unverified):
- Import stays bare `from rocket_lib import run_staging` — only path setup added (gui.py:29 mirror).
- Smoke case (test_call.py:4-17) untouched.
- No edits to rocket_lib.py / gui.py bridge code.
- No DLL-build task in this plan (machine blocker; 01-04 rebuilds).
- No TESTING.md edits beyond the Run Commands line.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test_call.py -> interface/rocket_lib.py | Import path constructed from `__file__` (repo-local, fixed); no user input involved |
| rocket_lib.py -> build/librocket.dll | Module-level ctypes.CDLL load (rocket_lib.py:22); DLL provenance not validated (FIX-03, Phase 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05-01 | Tampering | test_call.py sys.path.insert | low | mitigate | Insert only the repo-local `interface/` dir derived from `__file__` (gui.py:29 pattern); no environment/CWD-derived path component, so no path-injection surface |
| T-01-05-02 | Tampering | ctypes DLL load (rocket_lib.py:22) | medium | accept | Pre-existing documented machine blocker (DLL absent at rest); same acceptance as T-01-02 (plan 01-01) — provenance validation deferred to FIX-03 (Phase 3) |
| T-01-05-SC | Tampering | package installs | low | accept | No new packages — stdlib-only change; Package Legitimacy Audit N/A |
</threat_model>