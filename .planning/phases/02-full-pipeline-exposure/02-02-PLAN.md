---
phase: 02-full-pipeline-exposure
plan: 2
slug: full-pipeline-exposure
summary: Wire the GUI to the full pipeline: _run swaps to rocket_lib.run_full_pipeline (orbit, payload, ISP/k_s arrays, propellant indices, diameter mode), _last_v_circ feeds the "ΔV (auto)" label and the export, _auto_delta_v is deleted, and the results render with real ΔV + geometry — with an offscreen automated harness and a final interactive human walkthrough.
type: execute
wave: 2
depends_on:
  - 02-01
files_created: []
files_modified:
  - SRC/gui/gui.py
autonomous: false
requirements:
  - PIPE-01
phase_req_ids:
  - PIPE-01
user_setup: []
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "PIPE-01: `_run` (gui.py:1066) calls `rocket_lib.run_full_pipeline(n_stages=n, orbit_height=self.orbit_height.value(), payload_mass=self.pl_spin.value(), isp_list=isp_list, ks_list=ks_list, propellant_list=[sw.get_propellant_index() for sw in self.stage_widgets], diameter_setup=self.diameter_mode, user_diameter=self.diameter_spin.value())` — the `delta_v=` argument and the `_auto_delta_v()` feed are gone (D-02: the solver converges internally)."
    - "D-03/PIPE-01: after a run, every ResultCard renders REAL per-stage dv/diameter/length/volume numbers (%.2f km/s, %.2f m, %.2f m³ per the Phase 1 formatting contract) instead of '—'; the partial-state hint ('ΔV and geometry populate with full pipeline wiring (Phase 2).') no longer matches any card, so it is absent after a run."
    - "D-04: `_auto_delta_v` (gui.py:1002-1008) is deleted — no Python V_circ mirror remains anywhere in gui.py (grep for _auto_delta_v returns nothing)."
    - "D-01/D-04: the Setup 'ΔV (auto)' label shows '— km/s' pre-run and after any input change; after a successful run it shows f'{self._last_v_circ:.2f} km/s' where `_last_v_circ` = results['v_circ'] (the Fortran orbit_speed_calculator value returned through the new entry)."
    - "D-05: the export contract is preserved with the V_circ source: `_print_results` uses `dv = self._last_v_circ` for BOTH the 'Delta-V:' line and the filename `staging_{n}stage_dv{dv:.1f}_pl{pl}kg.txt` — label == exported Delta-V line == filename numeric value (the UAT-verified consistency)."
    - "Invalidation: `_on_inputs_changed` resets `self._last_v_circ = None` and refreshes the label to '— km/s' (and disables Save as today) — converged numbers are never displayed or exported against changed inputs."
    - "Bridge failure handling is verbatim (gui.py:1157-1159): any exception from the pipeline call → `QMessageBox.critical(self, 'Fortran Error', str(e))` + early return; input validation stays the same QMessageBox.warning pre-check."
    - "Bridge duplication guard (FIX-02/FIX-03 stay Phase 3): the new call goes through `import rocket_lib` (placed immediately after the sys.path hook at gui.py:29 — one import line, no third ctypes copy); the inline `_lib`/`run_staging` twin (gui.py:19-110) remains byte-untouched for Phase 3 dedup + MinGW work — no deletions, no edits inside it."
  artifacts:
    - "SRC/gui/gui.py — `import rocket_lib` line after the sys.path hook (gui.py:29)"
    - "SRC/gui/gui.py — StageInputWidget.get_propellant_index() (returns prop_combo.currentIndex() + 1, mirroring get_values' prop computation at gui.py:523)"
    - "SRC/gui/gui.py — _run success path: run_full_pipeline call, _last_v_circ = results['v_circ'], _update_auto_dv_label() refresh"
    - "SRC/gui/gui.py — _update_auto_dv_label reading _last_v_circ (None → '— km/s'); construction-time label '— km/s'; _on_inputs_changed reset"
    - "SRC/gui/gui.py — _print_results dv = self._last_v_circ + guard extension; _auto_delta_v deleted"
  key_links:
    - "wrapper dict keys <-> ResultCard data.get('dv'|'diameter'|'length'|'volume') (gui.py:600-603) — keys were fixed in 02-01; cards render without signature churn"
    - "results['v_circ'] <-> _last_v_circ <-> label AND export Delta-V line AND filename (single source of truth, D-01/D-05)"
    - "propellant indices (gui.py combos) <-> Geometry_calc select-case tables — geometry rows are only real when the ints are valid 1-8 (02-01 min() fallback secures stage slots)"
    - "hint condition `any(s.get(key) is None ...)` (gui.py:1130-1134) <-> wrapper packing all four keys — the hint self-disables"
  prohibitions:
    - "No Python-side physics: V_circ is never recomputed in gui.py; _auto_delta_v must not be resurrected under any name (D-04/PIPE-02)."
    - "The label, the exported Delta-V line, and the filename all stay V_circ-sourced (D-01/D-05) — never switched to the converged staging total."
    - "No stale results: input changes invalidate label + export; converged numbers never rendered/exported against mismatched inputs."
    - "No bridge dedup in this phase: the inline `_lib`/`run_staging` twin (gui.py:19-110) stays — FIX-02/FIX-03 are Phase 3; only the `import rocket_lib` line is added."
---

<objective>
Wire the GUI to the full pipeline: `_run` calls the new `rocket_lib.run_full_pipeline` (replacing the inline single-solver bridge call and its `_auto_delta_v()` ΔV feed), the Fortran-returned V_circ becomes the single source for the "ΔV (auto)" label, the export Delta-V line, and the suggested filename, the Python `_auto_delta_v` mirror is removed, and the ResultCards populate with real per-stage ΔV + geometry.

Purpose: Fulfill PIPE-01 — the GUI drives the full pipeline (orbit → payload → staging → thrust → geometry) through the single ctypes entry created in 02-01, replacing the interim Phase 1 mirror and the "—" placeholders, while preserving the UAT-verified label == Delta-V line == filename contract (D-01/D-05) and leaving FIX-02/FIX-03 to Phase 3.
Output: gui.py wired to the full pipeline with V_circ label/export chain and no `_auto_delta_v`.
</objective>

<execution_context>
@C:/Users/agusc/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/agusc/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-pipeline-exposure/02-CONTEXT.md
@.planning/phases/02-full-pipeline-exposure/02-PATTERNS.md
@SRC/gui/gui.py
@SRC/interface/rocket_lib.py

# Plan 2 of 2 — GUI wiring (PIPE-01); depends on 02-01 (entry + wrapper exist and are smoke-proven)
Single modified file: gui.py. The wrapper contract comes from 02-01 (keys: total_initial_mass, minimum_found, v_circ, stages[].dv/diameter/length/volume). The GUI's own inline `_lib`/`run_staging` twin (gui.py:19-110) is deliberately left in place — removing it is FIX-02/FIX-03 (Phase 3, coupled with the MinGW-path replacement); Phase 2 only adds `import rocket_lib` and switches the call site. The `diameter_mode` int (1/2/3) and `diameter_spin` value were stored on MainWindow in Phase 1 exactly for this handoff (gui.py:905, 924-930, 949-955); the per-stage propellant index comes from the StageInputWidget combos (the same 1-based index get_values already computes at gui.py:523).

Offscreen verification precedent: Phase 1 verified GUI behavior offscreen (STATE.md accumulated context). This plan's automated verifies use the same technique with a throwaway harness script in the pre-approved temp dir (C:\Users\agusc\AppData\Local\Temp\opencode) — no test files are committed (tests deferred by project decision).
</context>

<tasks>

<task type="auto">
  <name>02-02-T1: Switch _run to the full pipeline and populate results (PIPE-01, D-02, D-03)</name>
  <commit>feat(gui): run full pipeline from the GUI</commit>
  <files>SRC/gui/gui.py</files>
  <read_first>
    SRC/gui/gui.py — _run (1066-1159), the sys.path hook (28-29), get_values (521-537), StageInputWidget combos (343-355), diameter handoff state (905/924-930/949-955), ResultCard .get() rows (598-603), hint condition (1130-1137)
    SRC/interface/rocket_lib.py — run_full_pipeline signature + return dict (from 02-01)
    SRC/interface/C_Interface.f90 — run_full_pipeline arg semantics (orbit_height km, propellant ints 1-8, diameter_setup 1-3, user_diameter m)
  </read_first>
  <action>
    Add `import rocket_lib` on its own line immediately AFTER the `sys.path.insert(0, ...)` hook at gui.py:29 (so the interface/ dir is on sys.path first). Do NOT touch anything inside the inline bridge block (gui.py:19-110) — no edits, no deletions inside it (FIX-02/FIX-03 are Phase 3).

    Add a method `get_propellant_index(self)` on StageInputWidget returning `self.prop_combo.currentIndex() + 1` (1-based — the same index get_values already computes at gui.py:523; it feeds the Fortran propellant codes 1-8 whose geometry tables Geometry_calc.f90:19-90 branch on).

    In `_run` (gui.py:1066), replace ONLY the `results = run_staging(...)` call block (lines 1090-1097) with:
    `results = rocket_lib.run_full_pipeline(n_stages=n, orbit_height=self.orbit_height.value(), payload_mass=self.pl_spin.value(), isp_list=isp_list, ks_list=ks_list, propellant_list=[sw.get_propellant_index() for sw in self.stage_widgets], diameter_setup=self.diameter_mode, user_diameter=self.diameter_spin.value())`
    — the `delta_v=` argument disappears entirely (D-02: STAGING_LOOP converges internally). Keep the surrounding try/except verbatim (validation warning pre-check at 1081-1088 and QMessageBox.critical at 1157-1159 unchanged).

    In the success path (after `self._last_results = results`, gui.py:1146), add `self._last_v_circ = results["v_circ"]` — this is the D-01/D-04/D-05 single source on MainWindow, set on every successful run.

    No rendering changes: ResultCard already reads `data.get("dv"/"diameter"/"length"/"volume")` (gui.py:600-603) and the wrapper now packs all four keys, so the cards populate with %.2f formatting and the partial-state hint condition (gui.py:1130-1134) stops matching — the hint simply does not render after a run. Leave the hint code in place (inert when fully populated; it still guards genuinely missing keys).
  </action>
  <acceptance_criteria>
    - grep over gui.py: exactly one `import rocket_lib` line; `run_staging(` no longer appears in _run (only inside the untouched inline bridge definition at 80-110); `_auto_delta_v` still exists at this step (removal is T2).
    - Offscreen harness (temp dir): with QT_QPA_PLATFORM=offscreen, instantiate MainWindow, set orbit=500.0 / payload=5000.0 / 3 stages with valid LH2/LOX data, call _run(); assert `win._last_v_circ` is a finite float > 7.0 (V_circ for 500 km ≈ 7.6 km/s); assert results_layout contains exactly n ResultCard widgets after the run; assert NO QLabel in results_layout has the hint text 'ΔV and geometry populate with full pipeline wiring (Phase 2).'; assert bridge-dict dv/diameter/length/volume values are all finite and > 0 for every stage.
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile SRC/gui/gui.py; then the offscreen harness (throwaway script in C:\Users\agusc\AppData\Local\Temp\opencode\phase02_t1_harness.py, written by the executor per the acceptance criteria above, run with `$env:QT_QPA_PLATFORM='offscreen'; python C:\Users\agusc\AppData\Local\Temp\opencode\phase02_t1_harness.py`) — exits 0 only if all assertions pass. Precondition: `make all` ran in SRC/ (build/librocket.dll from 02-01).</automated>
  </verify>
  <done>A successful Run populates every card with real converged ΔV + geometry from the full pipeline; the hint is absent; _last_v_circ carries the Fortran V_circ; the inline twin is untouched.</done>
  <reversibility rating="reversible">Call-site swap + one accessor + one stored field; the old inline run_staging path is still present and identical; revert = restore the old call block and drop three lines.</reversibility>
  <dependencies>02-01 (run_full_pipeline entry + wrapper in the rebuilt DLL).</dependencies>
</task>

<task type="auto">
  <name>02-02-T2: V_circ label/export chain — remove _auto_delta_v (D-01, D-04, D-05)</name>
  <commit>feat(gui): feed ΔV label and export from the Fortran V_circ</commit>
  <files>SRC/gui/gui.py</files>
  <read_first>
    SRC/gui/gui.py — _auto_delta_v (1002-1008, delete), _update_auto_dv_label (1010-1012), its connections (825, 859-860), _print_results (1014-1064, esp. 1015-1022 and the dv use at 1035), _run success path (1146-1152, _last_v_circ added in T1)
    SRC/pre-staging-calcs/Orbit_calc.f90 — the formula being removed from Python (V_circ provenance stays Fortran-only, D-04)
  </read_first>
  <action>
    Delete the `_auto_delta_v` method entirely (gui.py:1002-1008) — no replacement, no rename (D-04: the Python mirror is removed; grep for '_auto_delta_v' must return zero matches when done).

    Construction: change the label initializer at gui.py:825 from `QLabel(f"{self._auto_delta_v():.2f} km/s")` to `QLabel("— km/s")` (pre-run partial-state idiom, same placeholder glyph/color semantics as the ResultCard rows).

    Rewrite `_update_auto_dv_label` (gui.py:1010-1012) to render from the stored Fortran source: if `getattr(self, '_last_v_circ', None)` is a finite number → `self.auto_dv_label.setText(f"{self._last_v_circ:.2f} km/s")`, else → `self.auto_dv_label.setText("— km/s")`. Keep the existing `self.orbit_height.valueChanged.connect(self._update_auto_dv_label)` wiring (gui.py:860) — on orbit change, _on_inputs_changed (fired first, connected at 859) clears _last_v_circ, then this handler renders the placeholder.

    Extend `_on_inputs_changed` (gui.py:997-1000): after the existing three lines, add `self._last_v_circ = None` + `self._update_auto_dv_label()` — input changes invalidate the label exactly like the results (no stale V_circ survives).

    In `_print_results` (gui.py:1014): extend the guard (line 1015) to also require a valid stored V_circ (`getattr(self, '_last_v_circ', None)` not None), and replace `dv = self._auto_delta_v()` (line 1019) with `dv = self._last_v_circ`. Nothing else in _print_results changes — the 'Delta-V:' line (1035) and the filename (1022) keep their existing formats, now sourced from the Fortran V_circ (D-01/D-05: label == line == filename).
  </action>
  <acceptance_criteria>
    - grep '_auto_delta_v' across SRC/gui/gui.py returns zero matches; grep '_last_v_circ' returns ≥ 4 (T1's success-path set + _update_auto_dv_label read + _on_inputs_changed reset + _print_results read).
    - Offscreen harness (extend the T1 script or a new temp script): (a) pre-run, `win.auto_dv_label.text() == '— km/s'`; (b) after `_run()` with orbit=500.0, `win.auto_dv_label.text() == f"{win._last_v_circ:.2f} km/s"` and that value equals results['v_circ'] (single source); (c) after changing `win.orbit_height.setValue(600.0)` (triggers _on_inputs_changed + label update), `win.auto_dv_label.text() == '— km/s'` again and `win._last_v_circ is None`; (d) `_print_results`' guard path: with _last_v_circ None it warns instead of writing a file.
    - Diff check: gui.py:19-110 (inline bridge) byte-identical to the pre-phase version.
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile SRC/gui/gui.py; then the offscreen harness for the four label/guard transitions (temp script, QT_QPA_PLATFORM=offscreen, `$env:QT_QPA_PLATFORM='offscreen'; python <temp>\phase02_t2_harness.py`) — exits 0 only if all assertions pass. Precondition: 02-02-T1 applied (run_full_pipeline import + call in place).</automated>
  </verify>
  <done>The label, export Delta-V line, and filename all source from the Fortran V_circ stored on _last_v_circ; _auto_delta_v no longer exists anywhere in the GUI; input changes reset the label to the placeholder; the inline bridge block is untouched.</done>
  <reversibility rating="reversible">Pure substitution of the ΔV source (label/export read _last_v_circ instead of the dead mirror); revert = reinstate _auto_delta_v and rewire three reads — the export format never changes either way (D-05 gate).</reversibility>
  <dependencies>02-02-T1 (_last_v_circ set in the success path) and 02-01 (v_circ key in the wrapper dict).</dependencies>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>02-02-T3: Interactive GUI walkthrough — full pipeline results, V_circ label chain, export</name>
  <what-built>
    The GUI now runs the complete Fortran pipeline on Run: per-stage masses/ratios/ν_e as before PLUS per-stage ΔV, diameter, length, and volume populated with real numbers (%.2f), the Setup 'ΔV (auto)' label showing the Fortran V_circ after a run (— before/after input changes), and the export Delta-V line + suggested filename using the same V_circ.
  </what-built>
  <how-to-verify>
    1. In SRC/ run `make gui` (builds the DLL first — required on this machine).
    2. Setup tab: orbit 500.0 km, payload 5000 kg, 3 stages — pick 'Liquid Hidrogen / Liquid Oxigen' + 'Staged Combustion' for EVERY stage. Do NOT deviate from LH2/LOX: it is one of only two propellants (with UDMH/N2O4, index 6) whose statistical geometry tables have populated rows in all three stage tables (stage-2 case 3 is a silently empty branch) — any other combo can yield empty/zero geometry that step 7's checks must catch. Confirm the 'ΔV (auto)' label reads '— km/s' before any run, and that dragging the orbit-height spinbox keeps it at '— km/s' (no stale value).
    3. Vehicle Configuration tab: switch between the three diameter modes — Statistically determined (1), Constant (2), User-specified (3, shows the 0.5-20 m box) — and set 2.50 m in mode 3. The chosen mode must affect the diameter column (mode 3 → all stages show 2.50 m; mode 2 → all stages show the same widest value; mode 1 → monotonic non-decreasing diameters).
    4. Run. The app auto-switches to Results: every card shows populated 'Stage ΔV' / 'Diameter' / 'Length' / 'Volume' values (numbers, not '—'), the partial-state hint is GONE, and 'Total Initial Mass' / 'Minimum confirmed' behave as before. A console log of 'Delta_V = ...' iterations and 'Minimum found' output from Fortran is expected (inherited STAGING_LOOP prints).
    5. The Setup label now shows e.g. '7.62 km/s'. Save Results: the Delta-V line in the file and the suggested filename (staging_3stage_dv7.6_pl5000kg.txt) must match the label value (rounded consistently — label 2 decimals, filename 1 decimal).
    6. Change orbit height → results clear, Save disables, label returns to '— km/s'. Run again → all repopulate with the new V_circ.
    7. Sanity: every card's Diameter / Length / Volume cells are POPULATED with real numbers — Diameter and Length in meters (0.5-20 m scale), Volume in m³ consistent with π/4·D²·L — never '—', never 0, never billions (an empty geometry-table branch shows exactly '—' or 0); sizes are the same scale as the vehicle mass; no 'WARNING: unknown ... propellant' lines in the console (propellant indices flow through).
  </how-to-verify>
  <resume-signal>Type 'approved' (or describe the failing behavior — include the label value, stage count, and mode used).</resume-signal>
</task>

</tasks>

## Verification (phase-level)

- 02-02-T1/T2 automated gates: `python -m py_compile SRC/gui/gui.py` + offscreen harness assertions (temp dir scripts, not committed — automated tests stay deferred per project decision).
- Phase gate: the T3 interactive checklist — every D-01..D-05 behavior verified by the user on `make gui`, plus geometry table sanity (modes 1/2/3) and no stale values after input changes.
- Cross-plan gate: `python SRC/test_call.py` still exits 0 after T1/T2 (bridge contract unchanged by GUI edits).

<success_criteria>
The GUI drives the full pipeline through the single ctypes entry: Results populate per-stage ΔV + geometry from the converged run (no '—', no hint), the label/export/filename chain is V_circ-sourced from Fortran with the UAT-verified consistency, `_auto_delta_v` no longer exists in Python, input changes invalidate cleanly, the inline bridge twin survives untouched for Phase 3, and the user confirmed all of it interactively (T3).
</success_criteria>

<output>
Create `.planning/phases/02-full-pipeline-exposure/02-02-SUMMARY.md` when done.
</output>

## Edge Probe Assumptions (spec-less fallback — ALL items FLAGGED)

Both ROADMAP requirements (PIPE-01, PIPE-02) were `unclassified`/`unresolved` by the deterministic detector — reviewed manually here, never auto-resolved. PIPE-02's mechanics are fully handled by plan 02-01; this plan closes PIPE-01.

| Probe item | Status | Disposition |
|---|---|---|
| PIPE-01 unclassified (GUI drives full pipeline) | COVERED-BY-TRUTH #1-#7 | FLAGGED-ASSUMPTION: the GUI passes orbit/payload/stage-count/ISP/k_s/propellant/diameter-mode and renders the converged outputs; nothing is hand-entered (D-10 holds); the sole ΔV displayed is V_circ for label/export (D-01) with per-stage converged ΔV in the cards (D-03). |
| PIPE-01 unclassified (thrust step visibility) | FLAGGED-ASSUMPTION | Thrust metrics (T, m_dot, t_burn, rt_burn) run inside STAGING_LOOP and stay internal — planner discretion (CONTEXT :26), reversible via pure output-packing later; no display surface exists today (card contract fixed in Phase 1). |
| Label semantics (pre-run / post-change) | FLAGGED-ASSUMPTION | '— km/s' pre-run and after input changes (partial-state idiom, PATTERNS §6); the value appears only when a completed run's Fortran V_circ exists — no stale or fabricated value is ever shown. |
| Propellant-index plumbing (Note B) | FLAGGED-ASSUMPTION | Combo indices reach Geometry_calc tables via the new entry; the deferred ISP/k_s authority question (sliders vs tables) is untouched — ISP/k_s still arrive as explicit arrays from the sliders. |
| Hint code left in place | FLAGGED-ASSUMPTION | The partial-state hint condition stays but self-disables when all four keys are packed; it still guards genuinely missing keys (partial population), consistent with Phase 1's D-05/D-06 semantics. |
| Inline bridge twin retained | FLAGGED-ASSUMPTION | gui.py:19-110 stays byte-identical (FIX-02 dedup + FIX-03 MinGW path are Phase 3); only an `import rocket_lib` line is added — the twin becomes dead-but-present code until then. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI event loop → rocket_lib.run_full_pipeline | Untrusted-ish input (spinbox/combobox values) crosses into the Fortran DLL; scalar/array buffers per the 02-01 contract |
| Fortran results → GUI display / export | Bridge return dict is rendered and written to a user-picked .txt path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | _print_results export path | low | mitigate | Unchanged Phase 1 contract: writes only to the QFileDialog-picked path; filename from numeric spinbox values + the Fortran V_circ (bounded 100-2000 km → ~7.0-7.9 km/s), no free-text path construction (ASVS V5) |
| T-02-07 | Tampering | stale-results display after input change | high | mitigate | _on_inputs_changed resets _last_v_circ → label '—' and disables Save; results layout already cleared — converged numbers can never render/export against changed inputs (ASVS V5 state integrity) |
| T-02-08 | Tampering | fabricated display values (NaN/garbage from bridge) | medium | mitigate | ResultCard's NaN guard (`value == value`, gui.py:573) keeps rendering '—' for non-finite values; 02-01 smoke asserts finite/>0 for all new keys pre-GUI |
| T-02-09 | Denial of Service | blocking pipeline call on UI thread | low | accept | Established Phase 1 concurrency contract (A6): solver ≤50 iterations, single-user desktop; no threading machinery introduced |
| T-02-SC | Tampering | package installs | low | accept | No new packages (ctypes stdlib only; PyQt6 installed) — Package Legitimacy Audit N/A |
</threat_model>