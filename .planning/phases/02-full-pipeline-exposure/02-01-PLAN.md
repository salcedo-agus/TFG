---
phase: 02-full-pipeline-exposure
plan: 1
slug: full-pipeline-exposure
summary: Add the full-pipeline bind(C) entry run_full_pipeline to C_Interface.f90 (payload → orbit → STAGING_LOOP → geometry per Main.f90 order, all module globals seeded), add the geometry store-back wiring to Geometry_calc.f90, and expose the single-entry wrapper run_full_pipeline in rocket_lib.py with a validating smoke hook in test_call.py.
type: execute
wave: 1
depends_on: []
files_created: []
files_modified:
  - SRC/interface/C_Interface.f90
  - SRC/pre-simulation-calcs/Geometry_calc.f90
  - SRC/interface/rocket_lib.py
  - SRC/test_call.py
autonomous: true
requirements:
  - PIPE-02
phase_req_ids:
  - PIPE-02
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PIPE-02: A SINGLE new bind(C) entry `run_full_pipeline` is exported from librocket.dll and callable via ctypes (hasattr probe on the loaded library passes); it is the only new entry — no second/third variant is added anywhere."
    - "D-02: The entry's body reproduces the console call order (Main.f90:11-17) exactly: Payload_Mass_calculator(Rocket) → orbit_speed_calculator (sets the V_circ global BEFORE STAGING_LOOP reads it at Stage_Optimization_Loop.f90:24) → STAGING_LOOP(Rocket) (converged ΔV loop, NOT a single STAGING call) → rocket_geometry_calculation(Rocket)."
    - "D-03: Per-stage Packed `dv_out(i)` = `Rocket%stage(i)%D_v` (Staging.f90:141 — converged, loss-inclusive distribution) and the smoke hook asserts `v_circ < sum(dv) < v_circ + 5` (DV_loss > 0 with V_rot=0, bounded by the console-path loss magnitude)."
    - "All module globals the pipeline reads are seeded from entry arguments before any call: payload_mass, number_of_stages, orbit_height (km, GUI units), diameter_setup, user_defined_diameter, first/second/third_stage_propellant_and_oxidizer with the min(i,n_stages) fallback for stages beyond n — no global the pipeline reads is left at its default 0 (combustion-cycle globals stay unseeded but are read by zero pipeline modules; no 'unknown propellant' default-case warnings, no uninitialized-vector garbage)."
    - "PIPE-02 (no physics reimplementation): `Rocket%rm_L` is set by calling the existing Payload_Mass_calculator (Payload_Mass_calc.f90:11-19), NOT by re-coding the PAF formula in the new entry; `V_circ` comes from orbit_speed_calculator, never computed in Python or assigned by hand."
    - "Geometry wiring (PATTERNS correction — Geometry_calc.f90 currently computes Diameter_vector/Longitud_vector as locals and never stores them): the subroutine gains ONLY a store-back loop `Rocket%stage(i)%Diameter = Diameter_vector(i); Rocket%stage(i)%Length = Longitud_vector(i)` for i=1..Rocket%number_of_stages after line 105 — zero formula changes. `volume_out(i)` is derived in C_Interface as pi*Diameter**2*Length/4 (the exact Geometry_calc.f90:105 identity `V = pi/4·D²·L`), with `use constants` for pi (constants.mod already produced with Typical_Data.o — no Makefile change)."
    - "The existing `run_staging` entry (C_Interface.f90:9-101) and the Makefile are byte-untouched (FIX-02/FIX-03 are Phase 3); the new entry is added alongside run_staging in the same module."
    - "rocket_lib.py exposes `run_full_pipeline(n_stages, orbit_height, payload_mass, isp_list, ks_list, propellant_list, diameter_setup, user_diameter)` with an argtypes list whose count and order mirror the bind(C) signature exactly (scalars via byref POINTER, arrays via (c_double*n)/(c_int*n) buffers, no VALUE in Fortran); the return dict packs `total_initial_mass`, `minimum_found`, `v_circ`, and `stages:[{stage, m0, mf, mp, ms, k_m, k_s, k_L, nu_e, dv, diameter, length, volume}]` — the single Python contract the GUI consumes."
    - "test_call.py smoke hook: extended with a run_full_pipeline block (n=3, orbit=500.0 km, payload=5000.0 kg, isp=[400,350,300], ks=[0.10,0.15,0.20], propellant=[1,1,1] LH2/LOX, diameter_setup=1) that prints and asserts: v_circ within 1e-6 of the Orbit_calc.f90:10 formula value; per-stage dv/diameter/length/volume finite and > 0; volume == pi/4·D²·L per stage; sum(dv) in (v_circ, v_circ+5)."
  artifacts:
    - "SRC/interface/C_Interface.f90 — `run_full_pipeline` bind(C) entry (globals seeding, pipeline sequence, result packing incl. dv/diameter/length/volume, eq-26 minimum check reusing the run_staging duplication pattern, deallocate)"
    - "SRC/pre-simulation-calcs/Geometry_calc.f90 — store-back loop persisting Diameter/Length into Rocket%stage(i)"
    - "SRC/interface/rocket_lib.py — run_full_pipeline argtypes + wrapper function"
    - "SRC/test_call.py — full-pipeline smoke block with assertions"
  key_links:
    - "bind(C) signature in C_Interface.f90 <-> argtypes list in rocket_lib.py (count and order are the Fortran↔Python contract — a mismatch segfaults, never raises)"
    - "Module-global seeding (orbit_height, propellant ints, diameter_setup) <-> reads in Orbit_calc.f90 / Stage_Optimization_Loop.f90 / Geometry_calc.f90 (unseeded globals silently produce wrong V_circ, geometry warnings, or garbage vectors)"
    - "Geometry_calc store-back <-> packed diameter/length/volume (without it the new outputs would be stack garbage)"
    - "test_call.py assertions <-> freshly rebuilt build/librocket.dll (must run `make all` first — build/ is missing on this machine, STATE.md blocker)"
  prohibitions:
    - "No third ctypes bridge copy: the new entry is declared (argtypes) and wrapped exactly once, in rocket_lib.py — never duplicated into gui.py or any other file."
    - "No physics reimplementation: V_circ is never hand-computed/assigned in the new entry or in Python; rm_L comes from the existing Payload_Mass_calculator; the only arithmetic added is the D/L volume identity."
    - "run_staging (C_Interface + rocket_lib + gui.py inline twin) and the Makefile are not modified in this plan — bridge dedup and MinGW path are Phase 3 scoped work."
    - "Inherited console-path quirks are preserved, not silently 'fixed': the T_a=400.d0 hardcode (Stage_Optimization_Loop.f90:23) and the per-iteration 'Delta_V = ' console prints stay."
---

<objective>
Add the Fortran side of the single ctypes pipeline entry: a new `run_full_pipeline` bind(C) subroutine in `C_Interface.f90` that runs the complete console-path sequence (payload → orbit → staging loop → geometry) on the module globals seeded from its arguments, plus the one-store-back wiring in `Geometry_calc.f90` that makes diameter/length persist into the Rocket struct, plus the Python wrapper and a validating smoke hook.

Purpose: Fulfill PIPE-02 — the Fortran interface exposes the bindings needed to run the full pipeline from Python through ONE ctypes entry, reusing the existing modules (`Payload_Mass_calculator`, `orbit_speed_calculator`, `STAGING_LOOP`, `rocket_geometry_calculation`) with no physics reimplementation. This plan produces and provably verifies the bridge chain headlessly; the GUI consumes it in plan 02-02.
Output: `run_full_pipeline` in C_Interface.f90 + store-back loop in Geometry_calc.f90 + `run_full_pipeline` wrapper + argtypes in rocket_lib.py + asserted smoke block in test_call.py.
</objective>

<execution_context>
@C:/Users/agusc/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/agusc/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-pipeline-exposure/02-CONTEXT.md
@.planning/phases/02-full-pipeline-exposure/02-PATTERNS.md
@SRC/Main.f90
@SRC/interface/C_Interface.f90
@SRC/interface/rocket_lib.py
@SRC/staging/Stage_Optimization_Loop.f90
@SRC/pre-staging-calcs/Orbit_calc.f90
@SRC/pre-staging-calcs/Payload_Mass_calc.f90
@SRC/pre-simulation-calcs/Thrust_calc.f90
@SRC/pre-simulation-calcs/Geometry_calc.f90
@SRC/staging/Rocket_Types.f90
@SRC/staging/Staging.f90
@SRC/inout/Typical_Data.f90
@SRC/Makefile
@SRC/test_call.py

# Plan 1 of 2 — Fortran bind(C) entry + Python wrapper + smoke proof (PIPE-02)
Depends on nothing (Phase 1 delivered the GUI shell and the run_staging precedent). One important correction to the pattern map, verified by grep over SRC/: `rocket_geometry_calculation` (Geometry_calc.f90:1-106) computes `Diameter_vector`/`Volume_vector`/`Longitud_vector` as LOCAL arrays and never assigns `Rocket%stage(i)%Diameter` / `%Length` — the PATTERNS claim "sets stage Diameter/Length" (PATTERNS :33) is wrong. This plan adds the minimal store-back loop as wiring. `Stage_t` has no Volume field (Rocket_Types.f90:22-23), so volume is derived in C_Interface via the exact geometry identity `V = pi/4·D²·L` (Geometry_calc.f90:105), with `use constants` for pi (constants.mod is emitted from Typical_Data.f90, already a compile dependency of C_Interface.o — Makefile rule 5, lines 127-130, needs NO change).

Note B resolution (CONTEXT Deferred / PATTERNS :240): the geometry select cases read first/second/third_stage_propellant_and_oxidizer (Geometry_calc.f90:19-90). This plan passes per-stage propellant indices from the GUI combos (1-based, matching the Fortran 1-8 codes) with a min(i,n_stages) fallback in the entry so no index is ever 0 and no default-case warning fires. This does NOT touch the deferred ISP/k_s authority decision — ISP/k_s still arrive as explicit per-stage arrays exactly as today.
</context>

<tasks>

<task type="tracer">
  <name>02-01-T1: Add run_full_pipeline bind(C) entry + geometry store-back (D-02, D-03, PIPE-02)</name>
  <commit>feat(fortran): add full-pipeline bind(C) entry to C_Interface</commit>
  <files>SRC/interface/C_Interface.f90, SRC/pre-simulation-calcs/Geometry_calc.f90</files>
  <read_first>
    SRC/interface/C_Interface.f90 — the run_staging analog to extend (9-101); do not alter it
    SRC/Main.f90 — console-path call order to reproduce (lines 11-17)
    SRC/staging/Stage_Optimization_Loop.f90 — STAGING_LOOP behavior, globals read at lines 12/24, T_a=400 hardcode at 23 (inherit, do not fix)
    SRC/pre-staging-calcs/Orbit_calc.f90 — orbit_speed_calculator sets V_circ (km/s)
    SRC/pre-staging-calcs/Payload_Mass_calc.f90 — Payload_Mass_calculator sets Rocket%rm_L
    SRC/pre-simulation-calcs/Geometry_calc.f90 — locals computed, nothing stored; store-back goes after line 105
    SRC/staging/Rocket_Types.f90 — Stage_t fields available for packing (D_v:19, T/m_dot/t_burn:16-18, Diameter/Length:22-23)
    SRC/inout/Typical_Data.f90 — module globals: orbit_height (37), payload_mass (38), number_of_stages (39), propellant ints (41-43), diameter_setup (49), user_defined_diameter (50), V_circ (36); constants module (pi, g_0, Radius, lines 1-6)
    SRC/staging/Staging.f90 — stage(i)%D_v set at line 141, m_i at 147
    SRC/Makefile — read-only reference: C_Interface.o already depends on Typical_Data.o (constants.mod), no edits
  </read_first>
  <action>
    In `SRC/interface/C_Interface.f90`, add `use constants` to the module's use list (for `pi`), and add a NEW subroutine `run_full_pipeline` after `run_staging` (leave lines 9-101 byte-untouched). Bind(C) signature, scalar args `intent(in/out)` with NO `VALUE`, arrays explicit-shape — parameter order exactly:

    n_stages (integer c_int, in) · orbit_height_in (real c_double, in) · payload_mass_in (real c_double, in) · isp_in(n_stages) (c_double, in) · ks_in(n_stages) (c_double, in) · propellant_in(n_stages) (integer c_int, in) · diameter_setup_in (integer c_int, in) · user_diameter_in (real c_double, in) · m0_out(n_stages) (c_double, out) · mf_out(n_stages) (out) · mp_out(n_stages) (out) · ms_out(n_stages) (out) · km_out(n_stages) (out) · ks_out(n_stages) (out) · kl_out(n_stages) (out) · nu_e_out(n_stages) (out) · dv_out(n_stages) (out) · diameter_out(n_stages) (out) · length_out(n_stages) (out) · volume_out(n_stages) (out) · total_m0_out (c_double, out) · v_circ_out (c_double, out) · minimum_found_out (integer c_int, out) — bound as `bind(C, name="run_full_pipeline")`.

    Body, in this exact order:
    1. Seed the module globals from arguments: `payload_mass = payload_mass_in`; `number_of_stages = n_stages`; `orbit_height = orbit_height_in` (km, GUI units — the console path and DV_loss use the same km convention); `diameter_setup = diameter_setup_in`; `user_defined_diameter = user_diameter_in`; `first_stage_propellant_and_oxidizer = propellant_in(1)`; `second_stage_propellant_and_oxidizer = propellant_in(min(2, n_stages))`; `third_stage_propellant_and_oxidizer = propellant_in(min(3, n_stages))` — the min() fallback guarantees a valid 1-8 index for every stage slot, so the Geometry_calc select cases never hit `case default`.
    2. Build the Rocket struct exactly like run_staging lines 44-51: `Rocket%number_of_stages = n_stages`, `allocate(Rocket%stage(n_stages))`, per-stage `Rocket%stage(i)%ISP = isp_in(i)` and `Rocket%stage(i)%k_s = ks_in(i)`.
    3. `call Payload_Mass_calculator(Rocket)` — sets Rocket%rm_L through the console-path subroutine (Payload_Mass_calc.f90:11-19); do NOT copy the inline PAF formula into this entry (PIPE-02 reuse).
    4. `call orbit_speed_calculator` — sets the V_circ global; MUST run before STAGING_LOOP (D-02).
    5. `call STAGING_LOOP(Rocket)` — the converged loop (D-02), NOT a single `call STAGING`. Note: STAGING_LOOP calls stage_Thrust_calculator internally (Stage_Optimization_Loop.f90:36), so thrust metrics are computed for free but stay internal — do not pack them (planner discretion, CONTEXT:26). Inherit the T_a=400.d0 hardcode (line 23) and the per-iteration console prints (lines 43-46) as-is.
    6. `call rocket_geometry_calculation(Rocket)`.
    7. Result-packing loop i=1..n_stages: m0_out(i)=Rocket%stage(i)%m_0, mf_out(i)=%m_f, mp_out(i)=%m_p, ms_out(i)=%m_s, km_out(i)=%k_m, ks_out(i)=%k_s, kl_out(i)=%k_L, nu_e_out(i)=%nu_e (same mapping as run_staging lines 64-73); THEN the new outputs: dv_out(i)=Rocket%stage(i)%D_v (converged per-stage ΔV, D-03), diameter_out(i)=Rocket%stage(i)%Diameter, length_out(i)=Rocket%stage(i)%Length, volume_out(i)=pi*Rocket%stage(i)%Diameter**2*Rocket%stage(i)%Length/4.d0 (the Geometry_calc.f90:105 identity, using `pi` from the constants module).
    8. total_m0_out = Rocket%rm_0; v_circ_out = V_circ (D-01/D-04: the label + export source).
    9. The eq-26 minimum check — reproduce the run_staging lines 81-91 pattern verbatim (same guard, same check expression, same check_count semantics → minimum_found_out) — reuse the established duplication, do not invent a third variant.
    10. `deallocate(Rocket%stage)` (mirror run_staging line 99).

    In `SRC/pre-simulation-calcs/Geometry_calc.f90`, after the `Longitud_vector = ...` assignment (line 105) and before `end subroutine`, add a store-back loop: for i=1, Rocket%number_of_stages: Rocket%stage(i)%Diameter = Diameter_vector(i) and Rocket%stage(i)%Length = Longitud_vector(i). This is wiring-only — do not touch any formula, select case, or the Diameter_setup logic (lines 19-104). Do not add a Volume field to Stage_t.

    Explicitly do NOT: modify run_staging; modify the Makefile; add `use` of staging/root_finding modules (all called subroutines are external — C_Interface.o needs no new .mod dependencies).
  </action>
  <acceptance_criteria>
    - `SRC/interface/C_Interface.f90` compiles with gfortran -O2 -Wall with no new warnings; `make all` in SRC/ links librocket.dll with the new symbol.
    - A ctypes probe `hasattr(lib, 'run_full_pipeline')` on the rebuilt build/librocket.dll (with build/ added to the DLL search path) returns True.
    - `run_staging` source lines 9-101 byte-identical before/after the change (git diff shows only the single `use constants` header-line change plus the added `run_full_pipeline` subroutine).
    - `SRC/pre-simulation-calcs/Geometry_calc.f90` diff adds ONLY the store-back loop after line 105 — no formula or select-case lines changed (git diff visible).
  </acceptance_criteria>
  <verify>
    <automated>npm-installed python not required: run `make all` in SRC/ (builds build/librocket.dll at C:/Users/agusc/Desktop/TFG/TFG/build; uses gfortran per SRC/Makefile), then `python -c "import os,ctypes,sys; b=r'C:/Users/agusc/Desktop/TFG/TFG/build'; os.add_dll_directory(b); lib=ctypes.CDLL(os.path.join(b,'librocket.dll')); assert hasattr(lib,'run_full_pipeline'); print('OK run_full_pipeline exported')"`.</automated>
  </verify>
  <done>The new entry compiles, links into librocket.dll, and is callable by name via ctypes; run_staging and the Makefile are untouched; Geometry_calc gained only the store-back loop.</done>
  <reversibility rating="reversible">Additive bind(C) entry + additive store-back loop; both are new code paths that do not alter existing behavior (run_staging path identical); revert = delete the additions.</reversibility>
  <dependencies>None (Phase 1 artifacts: run_staging precedent, GUI shell).</dependencies>
</task>

<task type="auto" tdd="false">
  <name>02-01-T2: Add run_full_pipeline wrapper + asserted smoke hook (D-01, D-03, PIPE-02)</name>
  <commit>feat(bridge): add full-pipeline ctypes wrapper and smoke assertions</commit>
  <files>SRC/interface/rocket_lib.py, SRC/test_call.py</files>
  <read_first>
    SRC/interface/rocket_lib.py — run_staging argtypes (26-43) + wrapper (45-88): the analog; the NEW wrapper is added alongside, run_staging stays
    SRC/interface/C_Interface.f90 — the run_full_pipeline signature just added in T1 (argtypes must mirror it 1:1 in count and order)
    SRC/pre-staging-calcs/Orbit_calc.f90 — the V_circ formula for the smoke assertion (v_circ_out check)
    SRC/staging/Stage_Optimization_Loop.f90 — loss model: DV_new = V_circ + DV_loss/1000 - V_rot with V_rot=0 ⇒ sum of per-stage dv strictly exceeds v_circ
  </read_first>
  <action>
    In `SRC/interface/rocket_lib.py`, directly after the run_staging argtypes declaration (line 43), declare `lib.run_full_pipeline.restype = None` and the matching argtypes list — one `ctypes.POINTER(...)` entry per bind(C) parameter in the EXACT order fixed in 02-01-T1: c_int n_stages; c_double orbit_height; c_double payload_mass; c_double isp(n); c_double ks(n); c_int propellant(n); c_int diameter_setup; c_double user_diameter; c_double m0(n); c_double mf(n); c_double mp(n); c_double ms(n); c_double km(n); c_double ks_out(n); c_double kl(n); c_double nu_e(n); c_double dv(n); c_double diameter(n); c_double length(n); c_double volume(n); c_double total_m0; c_double v_circ; c_int minimum_found. This list is the Fortran↔Python contract — count and order must match the subroutine exactly (a mismatch segfaults, never raises).

    Add the wrapper function `run_full_pipeline(n_stages, orbit_height, payload_mass, isp_list, ks_list, propellant_list, diameter_setup, user_diameter)` below run_staging (lines 45-88 stay unchanged): scalars in as `ctypes.c_double()`/`ctypes.c_int()` wrappers and passed by `ctypes.byref(...)` (never byref a raw Python float); per-stage inputs as `(ctypes.c_double * n_stages)(*isp_list)` / `(ctypes.c_double * n_stages)(*ks_list)` / `(ctypes.c_int * n_stages)(*propellant_list)`; per-stage outputs as zero-initialized buffers `(ctypes.c_double * n_stages)()`; total_m0, v_circ as `ctypes.c_double()`; min_found as `ctypes.c_int()`; ONE `lib.run_full_pipeline(...)` call passing everything positionally in the argtypes order.

    Return a dict with keys: `total_initial_mass` (float total_m0.value), `minimum_found` (bool(min_found.value)), `v_circ` (float v_circ.value) — the D-01/D-04 label + export source — and `stages`: a list of per-stage dicts `{"stage": i+1, "m0", "mf", "mp", "ms", "k_m", "k_s", "k_L", "nu_e", "dv", "diameter", "length", "volume"}` built from the packed arrays by index, exactly the key set the GUI's ResultCard already reads via data.get() (gui.py:598-603).

    In `SRC/test_call.py`, APPEND a full-pipeline smoke block after the existing run_staging block (keep the run_staging block as-is): import `run_full_pipeline` from rocket_lib, call it with n_stages=3, orbit_height=500.0, payload_mass=5000.0, isp_list=[400.0, 350.0, 300.0], ks_list=[0.10, 0.15, 0.20], propellant_list=[1, 1, 1] (LH2/LOX — one of the two propellants with valid statistical geometry rows for ALL three stage tables, the other being UDMH/N2O4 index 6; Geometry_calc cases (1)/(1)/(1)), diameter_setup=1, user_diameter=2.0. Print v_circ and each stage's dv/diameter/length/volume. Then assert, in order: (a) `v_circ` equals the Orbit_calc.f90:10 formula sqrt(g_0*Radius**2/((Radius+orbit_height)*1000)) with g_0=9.80665, Radius=6378.0, orbit_height=500.0 within 1e-6 (V_circ provenance — D-04); (b) every stage's dv/diameter/length/volume is finite and > 0; (c) per-stage `volume` equals `3.141592653589793 * diameter**2 * length / 4` within 1e-9 (identity check); (d) `v_circ < sum(stage dv) < v_circ + 5.0` (loss-inclusive converged total — D-02/D-03; DV_loss/1000 is the loss bound); (e) `total_initial_mass > payload_mass` and `minimum_found` is a bool. Use plain `assert` statements (this is the manual smoke hook, not an automated test suite — tests stay deferred per project decision). Contingency for geometry fit-domain inversion: stage-1 LH2/LOX diameter crosses zero below ~11.6 t stage-1 mass and volume below ~37.7 t (Geometry_calc fit inversion); rough staging for these inputs lands ~80 t stage-1 mass (~2x margin), so asserts should pass — but if the `> 0` geometry asserts fail solely because of fit-domain inversion, re-run the smoke with payload_mass >= 20000.0 and record the input deviation in the executor summary.
  </action>
  <acceptance_criteria>
    - `python SRC/test_call.py` exits 0 with all five assertion groups passing and prints v_circ + per-stage dv/diameter/length/volume (real numbers, not NaN/garbage).
    - The wrapper is declared exactly once in rocket_lib.py; no run_full_pipeline copy exists anywhere else (grep across SRC/, excluding test_call's import-and-call usage).
    - rocket_lib.py's run_staging block (lines 17-88) unchanged; test_call.py's run_staging block unchanged.
  </acceptance_criteria>
  <verify>
    <automated>python SRC/test_call.py (PowerShell: `python .\SRC\test_call.py` from the repo root — requires the rebuilt DLL from T1, so T1 must complete first; it loads via rocket_lib's own build/ + MINGW_BIN path logic; exits non-zero on any failed assert).</automated>
  </verify>
  <done>The wrapper returns the full pipeline contract dict with v_circ and per-stage dv/diameter/length/volume; the smoke hook proves the chain Python → DLL → full pipeline → packed dict with numeric assertions against the physics identities.</done>
  <reversibility rating="reversible">Additive wrapper + additive smoke block; run_staging wrapper untouched; revert = delete additions.</reversibility>
  <dependencies>02-01-T1 (the entry must exist in the rebuilt DLL before the wrapper can load run_full_pipeline).</dependencies>
</task>

</tasks>

## Verification (phase-level)

- Per-task commit gate: T1 = `make all` + export probe; T2 = `python SRC/test_call.py` (assertions must pass).
- Build prerequisite (STATE.md blocker): `build/librocket.dll` is missing on this machine — every run starts with `make all` (or `make gui`) in SRC/, which also copies the MinGW runtime DLLs into build/.
- The GUI behavior (PIPE-01) is verified in plan 02-02; this plan proves the bridge headlessly.
- Phase gate for PIPE-02: single new entry, three source modules reused by call, all globals seeded from args, smoke asserts green.

<success_criteria>
The full pipeline is callable from Python through exactly one new ctypes entry `run_full_pipeline` that reproduces the console-path order on seeded globals, packs per-stage ΔV (converged, D-03), geometry (diameter/length stored by the new wiring, volume by the D/L identity), and the Fortran V_circ (D-01/D-04) — all proven by the asserted smoke hook; run_staging and the Makefile are byte-untouched for Phase 3.
</success_criteria>

<output>
Create `.planning/phases/02-full-pipeline-exposure/02-01-SUMMARY.md` when done.
</output>

## Edge Probe Assumptions (spec-less fallback — ALL items FLAGGED)

Both ROADMAP requirements (PIPE-01, PIPE-02) were `unclassified`/`unresolved` by the deterministic detector — reviewed manually here, never auto-resolved.

| Probe item | Status | Disposition |
|---|---|---|
| PIPE-02 unclassified (bindings shape) | COVERED-BY-TRUTH #1 | FLAGGED-ASSUMPTION: single entry `run_full_pipeline`; signature = run_staging pattern widened with orbit_height/diameter_setup/user_diameter inputs + v_circ/rt_burn-scale outputs per planner discretion (D-02 Agent's Discretion); verified by the smoke asserts. |
| PIPE-02 unclassified (module reuse) | COVERED-BY-TRUTH #5 | FLAGGED-ASSUMPTION: the four pipeline subroutines are called, not re-implemented; the ONLY new arithmetic is the D/L volume identity (a geometric identity, not physics). |
| Geometry store-back (PATTERNS correction) | FLAGGED-ASSUMPTION | The pattern map claimed Geometry_calc stores D/L into Rocket%stage; grep proves it does not. Store-back loop added as wiring-only; this is the documented correction. |
| Note B — geometry propellant tables | FLAGGED-ASSUMPTION | Resolved by passing combo-derived propellant indices with min(i,n) fallback; the deferred ISP/k_s authority question is NOT touched (explicit ISP/k_s arrays continue). |
| Inherited quirks (T_a=400, loop prints) | FLAGGED-ASSUMPTION | Preserved verbatim per console-path parity; GUI will inherit console prints (acknowledged, CONTEXT). |
| Volume key semantics | FLAGGED-ASSUMPTION | No Stage_t volume field; volume = pi/4·D²·L derived in the entry — byte-equal by construction to Geometry_calc.f90:105 (Volume_vector = pi/4·D²·L). |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python (rocket_lib.py) → Fortran DLL | ctypes call boundary; scalar/array buffers cross it per the argtypes contract — a mismatched signature corrupts memory instead of raising |
| Repo → build/librocket.dll | DLL loaded from the local build/ dir by absolute path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | build/librocket.dll load path | medium | mitigate | Loads only the locally-built DLL from the repo's build/ dir (never remote, never a downloaded artifact); `make all` regenerates it from source; swap requires local write access — out of the desktop threat model (ASVS L1: no network surface) |
| T-02-02 | Information Disclosure | test_call.py / export output | low | accept | Smokes print design numbers to the local console only; no network path exists in the app |
| T-02-03 | Tampering | argtypes ↔ bind(C) contract | high | mitigate | Single declaration point in rocket_lib.py mirrored 1:1 against the Fortran signature; smoke hook asserts numerics (finite, >0, identity checks), so a wrong mapping crashes the smoke before any GUI use — contract drift is caught at build/test time (ASVS V5 validation) |
| T-02-04 | Denial of Service | STAGING_LOOP convergence | low | accept | Hard-bounded (≤50 iterations, 1e-6 tolerance); single-user desktop, blocking call is the established Phase 1 contract (A6) |
| T-02-05 | Tampering | propellant index into select-case | low | accept | Index is combo-derived 1..8 by construction (combo has 8 entries); min() fallback keeps all slots in-range; no user free-text input reaches Fortran |
| T-02-SC | Tampering | package installs | low | accept | No new packages — ctypes (stdlib) only; PyQt6 already installed; Package Legitimacy Audit N/A |
</threat_model>